# M22 06C4B0 original-loader classification of opaque W3D evidence

The 06C4A read-only checker now passes only opaque in-memory W3D payloads to a fresh original `WW3DAssetManager::Load_3D_Assets(FileClass&)` CPU process over stdin. Each process is size- and time-bounded; child diagnostics are captured, not printed. The helper emits only `accepted` or `rejected`; crash, timeout or ambiguous output is `unresolved` and cannot close a family decision. Owned controls prove a valid W3D packet is accepted, an invalid chunk is rejected, a mixed owned BIG isolates only the opaque entry, and a SHDMESH chunk in the valid prefix of an opaque entry forces `unknown` rather than a false absence conclusion. No retail entry is extracted to disk.

For the available ignored Zero Hour archive, the same 4,429 `.w3d` entries yielded five opaque payloads; all five were rejected by the original loader, with zero crashes/timeouts/unresolved results and zero SHDMESH candidates in valid prefixes or malformed headers. The composite source-qualified result is `source_wwshade_required=no` **for this selected archive only**. The generic chunk-only result remains `unknown` as a conservative standalone audit. Base Generals archive coverage is not configured, and the enabled WWShade branch and complete owned frame are not yet runtime-qualified; C4 and M22 remain pending.

No private names, paths, bytes, hashes or model identities are emitted or committed. Retail data were read only from the existing ignored symlink, with no file copy, chmod or write. No product source behavior changed; the only C++ addition is an opt-in source-loader test mode. GCC/Clang Debug and GCC/Clang sanitized focused branch/audit/loader tests pass, with leak detection active on the sanitized source-only controls. All four final native builds and full asset-free host suites pass 184/184 sequentially, including original-source identity, ABI, provider-removal and dependency-ledger checks. No physical GPU claim is made by this read-only classification slice.
