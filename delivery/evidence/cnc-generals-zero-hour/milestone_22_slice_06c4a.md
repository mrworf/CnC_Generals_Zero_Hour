# M22 06C4A read-only retail W3D chunk-family audit evidence

The checker reads explicitly selected BIG archives with read-only file mapping, validates header/table/entry/chunk bounds, and reports only numeric top-level W3D family counts. It never prints archive/entry names, private paths, payload bytes or hashes, including when a caller-supplied path cannot be opened. The owned test proves mesh/HLOD/SHDMESH counts, the `yes` requirement signal, malformed BIG and W3D rejection, explicit opaque-entry `unknown` classification, and path-safe errors. The authored disabled WWShade branch test remains green; it verifies enabled `SHD_FLUSH` macro expansion but does not claim enabled runtime behavior.

The available ignored Zero Hour archive was examined read-only. It contains 4,429 `.w3d` entries: 4,424 boundedly parsed and five explicitly classified opaque/malformed. The parsed top-level aggregate is 46,662 chunks: type 0x0000=37,735; 0x0100=3,383; 0x0200=1,874; 0x0280=4; 0x0500=2; 0x0700=3,398; 0x0740=266; type 0x0B00 (SHDMESH)=0. Strict mode rejects the archive at the first opaque chunk; explicit report mode yields `wwshade_required=unknown`, **not** `no`. This proves only that no SHDMESH was seen in parseable entries of this one archive. Five opaque entries and separately unconfigured base Generals archive coverage remain C4 gates. The earlier original GameClient retail model probe passes but samples only one model, so it cannot close the family audit.

No retail symlink content was copied, changed, chmod'ed, hashed, or committed. No original WWShade branch or product behavior was modified. C4B must classify the opaque entries using original-source loading or another validated wrapper and inspect base archive coverage before deciding whether enabled WWShade runtime support is required. C4, later M22 slices and milestone acceptance remain pending.

All four final native builds pass. The focused branch-selection and owned chunk-audit tests pass on GCC/Clang Debug and GCC/Clang sanitized configurations. Each full asset-free host suite passes 183/183, including source identity, ABI, provider-removal and dependency-ledger checks. Two concurrent Debug suite runs initially collided on the LAN two-process socket test; sequential host reruns passed 183/183. The sanitized host suites each passed 183/183 with leak scanning active for these non-GPU tests. No physical GPU claim is made by this read-only audit slice.
