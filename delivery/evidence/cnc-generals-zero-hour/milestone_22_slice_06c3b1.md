# M22 slice 06C3B1 — original WW3D initialization and audio-source CPU state

Canonical `ww3d.cpp` now owns full/lite `WW3D::Init` and `Shutdown` in the CPU configuration, including source static-sort-list publication/reset, original optional Dazzle INI, original asset/predictive-LOD/debug release, and source full-init animation-sound-manager call. Original `animatedsoundmgr.cpp` replaces two previous placeholder methods and owns sound INI parsing, uppercase animation-name indexing, lookup, and the null-library trigger path. Native Windows/DX8/timer calls remain on the native branch; CPU full init requires a live public GPU edge and rejects an external native window before publishing state. The original lite branch does not advertise `IsInitted`; production `W3DDisplay::init` must use full initialization, with its actual window/device selection in 07. No Begin/End frame or physical pass claim is made here (B2/B3).

The owned original W3D animation and INI witness proves full versus lite, current/default static-list pointer identity and null after teardown, missing optional configuration, original embedded sound bone lookup, no-audio-library trigger, duplicate/full-without-edge/window rejection, malformed sound configuration unwind, resource ownership and repeated full init. Source `WW3D::Shutdown` releases the global asset manager; the witness reloads the original W3D through that manager after lite shutdown before querying its animation. Direct sanitizer evidence exposed a preexisting overlapping `strcpy` in the original WWLib `strtrim` reached by INI parsing. Its exact in-place semantics now use `memmove`, with a direct leading/trailing whitespace regression. Source and support provider-removal controls reject omission of original `animatedsoundmgr.cpp`, `ww3d.cpp`, and `trim.cpp`; ledger hashes include both new reached providers and the updated WW3D source.

GCC/Clang Debug full non-LAN **158/158** each, LAN **4/4** each outside UDP sandbox, GCC/Clang focused ASan/UBSan/LSan original-WW3D **4/4** each outside ptrace sandbox, explicit Khronos-validation Vulkan cumulative GPU **8/8**, checked dependency ledger, CPU identity and sound/trim provider removal all pass. Read-only retail content unchanged. B2 still owes explicit public pass load/clear/depth semantics, B3 original Begin/End, C3C original scene/viewport/static sequencing, 07 production display attachment, 08 retail recordings, and 09 retail Vulkan/visual/full acceptance.
