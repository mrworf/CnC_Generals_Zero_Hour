# M22 slice 06C3B3A — original texture-frame update and disabled memory log

The canonical `TextureLoader::Update` CPU configuration now executes the original per-frame `TextureBaseClass::Invalidate_Old_Unused_Textures` body from the shared original `texture.cpp`. Foreground texture tasks remain synchronous: an empty queue invokes no network callback, suspended updates skip expiration, and no new queue/scheduler was created. The authored thumbnail gate, sync-time age (`>` threshold), rapid-reload extended inactivation, override time, texture-applied count reset, and procedural exclusion survive. An enabled thumbnail walk without the original asset manager fails explicitly before publication. The canonical `wwmemlog.cpp` is linked in its authored Unix disabled-log profile; Linux-specific changes are limited to include case/Windows header guards, thread-ID acquisition, and the null disabled mutex result. Its frame counters remain zero, without invented allocation tracking.

An owned original `WW3DAssetManager`/TGA fixture loads a `TextureClass` through the original `TextureLoader` into `OriginalGpuEdge`/Recording, checks retention at the threshold, suspension, exact expiration, invalidated physical-owner rejection, reupload with a new handle, rapid-reload extension, override, thumbnail-disabled retention, procedural exclusion, no callback on an empty queue, zero memory-log counters, all file-owner return, and zero physical resources after teardown. A separate process checks the missing-manager negative. The original Win32 thumbnail lookup remains explicitly unavailable and is never used as the successful image loader.

GCC and Clang Debug full non-LAN suites: **161/161 each**. LAN suites: **4/4 each**, sequentially to avoid shared UDP port collision. Focused source/init/negative ASan+UBSan+LSan: **3/3 each** on GCC and Clang. Explicit Vulkan validation-layer cumulative GPU: **8/8**. Original CPU identity and texture-frame provider-removal checks pass; dependency ledger check passes. The post-suite test-only physical-owner assertion was rebuilt and rerun on GCC/Clang, both sanitizer configurations, and the GPU fixture configuration. Private retail content was not modified.

This is CPU frame preparation only: the original `WW3D::Begin_Render`/`End_Render` physical lifecycle and presentation are B3B, source scene/camera partial clears are C3C, production display is 07, retail recordings 08, and retail Vulkan/visual/full acceptance 09.
