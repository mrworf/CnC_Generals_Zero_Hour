# M22 slice 06C1A — canonical original sort-state layout and replay

The original `RenderStateStruct` field order and refcounting constructor/destructor/assignment were extracted unchanged in semantics from the native `dx8wrapper.h` into one shared header included by mutually exclusive native and Linux CPU branches. No layout fork or new generation field was introduced. Linux `DX8Wrapper::Get_Render_State`, `Set_Render_State` and `Release_Render_State` snapshot/replay the existing single original CPU-selected shader/material/texture/light/world/view/vertex/index owner; they do not introduce an adapter-owned cache. The original 16-bit source ranges, buffer types and unsupported secondary stream/required texture stages are checked before selected-state mutation, and original light validation is shared by the same wrapper method and snapshot preflight.

An original asset-manager-loaded mesh supplies the captured state. Its material, texture, vertex/index refs each increment exactly once on snapshot copy and return on destruction; original engine buffer refs remain owned by the selected wrapper. Invalid buffer type, stage 2 and light fail before replacing selected material or stage state; no active edge rejects capture. After source reset, the original state replays the identical 16-bit index bytes and a real physical indexed draw. Injected draw failure leaves source selection available for retry. The same source snapshot then reuploads/rebinds and draws those bytes through a **new** original GPU edge generation, while each physical edge releases its resources and the caller-owned targets/depth return to zero. A structural guard verifies exactly one shared declaration, its original field order, inclusion by both branch configurations, CPU production compile selection, and simulated shared-header removal/duplicate declaration rejection. Existing ABI/link-map/provider controls pass; the original sorter still remains typed until 06C1B, so this is not sorting or 06C acceptance.

GCC and Clang Debug non-LAN **153/153** each; LAN **4/4** each outside UDP sandbox; focused GCC/Clang ASan/UBSan/LSan original-W3D/ABI **10/10** each outside ptrace sandbox; explicit `VK_LAYER_KHRONOS_validation` Vulkan GPU **7/7** with zero Validation Error/VUID; source identity/provider-removal and checked dependency ledger pass. The read-only retail symlink remains unchanged.
