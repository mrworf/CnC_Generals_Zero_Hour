# M22 06C4D0 original material-pass abort ownership evidence

The first Recording source-scene negative exposed retained original `MatPassTaskClass` owners: after a dynamic cull-pass upload and draw failure, the mesh reference count rose from two during scene ownership to three and four across retries, and remained three after removal. Original `WW3D::Render` correctly aborted and invalidated the FVF containers; their destructors had not drained visible or delayed material-pass lists. The Linux-only destructor cleanup now deletes only abandoned tasks. Their original destructors release their mesh and pass references, while successful `Render_Procedural_Material_Passes`/delayed flush paths retain their authored order and ownership.

The focused generated source scene asserts a positive immediate, delayed and cull frame; then injected delayed draw, cull dynamic upload and cull draw failures each abort the frame, leave no active Recording pass, restore the scene mesh/pass to two/one refs, and allow a fresh frame. Removing the scene object leaves exactly one caller mesh ref. GCC/Clang Debug and sanitized source-only tests pass with leak detection active. The physical fixture additionally passes four host Vulkan validation generations with category-specific pixels; C4D retains its separate 30/30 repeated-validation and full-frame acceptance gate. No original class layout, Windows branch, production shader configuration or retail asset changed.

All four full native builds and sequential asset-free suites pass 187/187; source identity, ABI, provider-removal and the updated original-source dependency ledger are included. The Clang-sanitized physical CTest reports the same external Vulkan D-Bus LeakSanitizer allocations as the previously accepted mixed-scene control. With only physical GPU leak detection disabled (`ASAN_OPTIONS=detect_leaks=0`), the Clang ASan/UBSan Vulkan validation test passes; source-only LSan remains enabled and passes. This does not claim the driver D-Bus allocations are an engine leak or that C4D is accepted.
