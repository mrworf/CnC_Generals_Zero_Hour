# M22 slice 06B2A: original skinned FVF and material-source physical semantics

The canonical original `DX8_FVF_XYZNDUV2=0x252` and `FVFInfoClass` now select the exact position/normal/BGRA diffuse/UV0/UV1 vertex layout and a bounded matching unlit/lit shader variant. The original `VertexMaterialClass` and `DX8Wrapper` still issue COLORVERTEX and ambient/diffuse/specular/emissive material-source selectors. The device-edge vertex uniform maps those selected source values and preserves material fallback when COLOR2 is absent or COLORVERTEX disabled. This follows [Microsoft's per-vertex color state](https://learn.microsoft.com/en-us/windows/win32/direct3d9/per-vertex-color-state) and [documented absent-color fallback](https://learn.microsoft.com/en-us/windows/win32/direct3d9/diffuse-lighting). No device adapter selects a replacement material, geometry, skin, or pass order.

Recording asserts five exact original FVF attributes/offsets/stride, source-selected COLOR1/COLOR2/material selectors and uniform values, rejection of an unsupported selector without drawing, typed unsupported 0x152 variant, and prepared-state retry. Direct original source `VertexMaterialClass` + original buffer/W3D texture witnesses produce real validation-layer Vulkan pixels for 0/1/2 stages, COLOR1 RGB/alpha, absent COLOR2, COLORVERTEX disabled, and two device generations. COLOR1 pixels are checked against the source material and light selection; no synthetic stage/pass forces binding. The original HLOD, HTree, deformation and skin category remain explicitly **unaccepted** until 06B2B.

GCC and Clang Debug full builds pass 146/146 non-LAN and 4/4 LAN checks each; the fixed-port LAN tests ran sequentially after parallel invocation collided. Both GCC/Clang sanitizer focused suites pass 5/5 each. All five GPU tests pass using explicit `VK_LAYER_KHRONOS_validation`, with zero Validation Error/VUID. The updated translator hash passes the dependency-ledger checks, source identity, ABI and provider-removal controls in the full suites. Retail symlink untouched/read-only. 06B2B, B3/B4, 06C and later scene/retail/final Vulkan gates remain mandatory.
