# M22 06C4D owned original procedural-pass scene evidence

The generated, asset-free fixture renders through original `WW3D::Render(SimpleSceneClass, CameraClass)`, not direct polygon test calls. Its source `RenderHookClass` pushes and pops immediate, delayed-only and cull-volume `MaterialPassClass` instances through the original `RenderInfoClass` scheduler. It retains the previously accepted rigid, HLOD skin, decal/translucent, sorted and two-level static scene. An original loader-built cull tree supplies the APT route. Recording traces assert source phase order and draw counts: immediate and inside-cull add a procedural draw, delayed-only draws through its delayed queue, and the outside cull omits the APT draw.

Physical RGBA pixels across BGRA8/RGBA8 targets and two extents distinguish the original baseline, base-only mesh, immediate, delayed and inside-cull frames. The outside cull exactly equals the base-only frame; removing the source object exactly restores baseline pixels. A missing generated object is rejected. Destroying the bound depth target aborts the source frame; rebinding a new target yields pixels exactly equal to the pre-failure outside-cull frame. Previously accepted source tests retain absent required decal texture, null material override, stage/bind/draw failure and retry, static/sorting order and target-recreation controls. The 06C4D0 prerequisite independently proved delayed/cull procedural task refcounts after injected draw/upload failures. The supplied-retail C4B1 audit establishes no reached SHDMESH requirement across both configured roots, while C4C proves the guarded enabled source branch compiles without changing disabled production behavior.

The final binary passes 30/30 fresh GCC and 30/30 fresh Clang host Vulkan validation processes. Every process renders four source/device generations, covering BGRA8/RGBA8 and two extents, without validation diagnostics. Focused GCC/Clang Debug and leak-capable source-only sanitizer controls pass. Clang ASan/UBSan physical validation also passes with only GPU leak detection disabled; the same external Vulkan D-Bus LSan allocations were independently reproduced in the previously accepted mixed-scene control, while source-only LSan remains enabled. Four final native builds and sequential full asset-free suites pass 187/187 each, including source identity, ABI, provider-removal, LAN and clean dependency ledger checks. No retail content, original class layout or production shader selection changed.

Together with accepted C4A/B0/B1 retail family classification and C4C guarded branch witness, this accepts C4 and the owned original-source 06C aggregate for the two supplied retail roots. It does not claim enabled WWShade runtime when no supplied source asset requires SHDMESH, arbitrary unseen retail content, original GameClient display/terrain/effects (07), retail campaign/skirmish consumers (08), or final visual acceptance (09).
