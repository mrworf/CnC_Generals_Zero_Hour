# M22 07B2A original preset-material CPU lifecycle evidence

Native `DX8Wrapper::Do_Onetime_Device_Dependent_Inits` calls `VertexMaterialClass::Init`, and native wrapper shutdown calls `VertexMaterialClass::Shutdown`. The Linux CPU-only `WW3D::Init`/`Shutdown` path omits that wrapper, leaving the original status-circle `PRELIT_DIFFUSE` preset null. In the pending 07B2 source probe, the first status-circle render faulted at `VertexMaterialClass::Get_Preset` before a draw. This slice restores only the same preset owner lifetime at the original WW3D CPU boundary; native behavior and class layouts remain unchanged.

The focused `original_w3d_source_init` test requests the authored prelit preset in lite, full and re-entry generations, verifies its two-reference ownership and prelit lighting state, and releases the caller reference. Its existing no-edge/full-init and malformed-animation-sound negatives still reject and unwind before reuse. Debug Recording edge teardown returns zero physical resources. Source identity and provider-removal witnesses remain in the full suites.

All four native presets build and pass 185/185 host asset-free tests each (GCC/Clang Debug and GCC/Clang ASan+UBSan); sanitizer CPU tests retain leak detection. The 07B2 status-scene test is excluded because its independent texture-stage translation remains pending. The three cross-domain ledger checks detect only the still-uncommitted 07B2 `W3DScene.cpp` and `linux_game_engine.cpp` changes; the `ww3d.cpp` hash is updated here. A clean aggregate ledger is required at 07B2 acceptance. No physical status-pixel claim is made by this prerequisite.
