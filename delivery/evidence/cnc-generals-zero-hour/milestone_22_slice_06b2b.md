# M22 slice 06B2B: original HLOD skin category and batch

The canonical original `DX8SkinFVFCategoryContainer::Render` now runs its visible-skin queue, `MeshClass::Get_Deformed_Vertices`, `MeshGeometryClass` bone/normal transforms, original `DynamicVBAccessClass` lock and 0x252 vertex copy, original texture/material/shader/light/world-identity decisions, 16-bit category index and physical draw. Its original `statistics.cpp` skin counters are compiled from the source provider (physical D3D-only statistics calls guarded), link/compile identity and provider-removal enforced. The Linux branch retains visible source skin/category tasks when upload/draw rejects before completion so the owning scene can abort the pass and retry; native source order is unchanged. The original empty procedural task queue is a no-op; actual nonempty passes remain 06B3.

Owned W3D bytes load through original asset manager into an original HLOD with HTree and source-owned skin child. CPU witnesses prove the HLOD bone transform changes three original deformed vertex positions, while a bare skin without HLOD or a separately authored malformed bone index rejects before deformation. Recording traces original HLOD→dynamic vertex access→material/light/world identity→exact `nd2_lit` shader→16-bit indexed draw; injected physical buffer upload and draw failures yield no draw and retry from the same source queue. A malformed HLOD child rejects before draw and original renderer invalidation releases its tasks. A second authentic two-child HLOD fixture totals 65,538 source vertices: original 65,535 cap partitions into **two** indexed source draws, counted as two original skin renders and 65,538 original skin vertices; device resources return to zero after teardown. Validation-layer SDL_GPU Vulkan pixels change under an original HLOD bone translation and restore when the bone returns, using real original texture/material producers. Earlier 0/1/2-stage direct 0x252 physical semantics are proven by 06B2A; this HLOD category positively reaches its authored one-stage case. The current prior retail material scenario reports FVF 274 only, so no claim that a retail skin scene has been rendered yet; 08 inventories/executes required retail scenes.

GCC/Clang Debug full builds each pass 147/147 non-LAN tests (including compile/link-map provider-removal and dependency-ledger hashes) and 4/4 LAN tests run sequentially due fixed UDP ports. GCC/Clang focused ASan/UBSan/LSan each pass 6/6 including the >65,535 batch. All five Vulkan GPU tests pass with explicit `VK_LAYER_KHRONOS_validation`, zero Validation Error/VUID. Retail symlink remains read-only and untouched. 06B3 additional/delayed/cull, 06B4 decal, 06C full scheduler and 07–09 integration/retail/final Vulkan remain mandatory; aggregate 06B is not complete.
