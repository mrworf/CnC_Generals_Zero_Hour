# M22 08E1B3B: portable generated VideoBuffer evidence

This slice binds the original `VideoBuffer` base metadata and `free` behavior
to a project-owned portable generated byte buffer. It validates the four
source pixel formats, bounded dimensions and pitch, allocation failure/retry,
lock/unlock and frame replacement controls, stale-free rejection, recreation,
and two-generation zero ownership. Bink decoding, W3D textures, raw Direct3D,
retail media and pixels remain deferred.

- Focused GCC and Clang provider/removal checks passed **2/2** each.
- Fresh non-GPU/non-LAN suites passed **221/221** in all six configurations;
  sandbox ASan broad tests use `detect_leaks=0` only for ptrace restrictions.
- Host leak focus passed **2/2** in GCC and Clang; physical Vulkan **1/1**;
  serial LAN **4/4** in all six configurations.
- Direct/registered ledger validation and `git diff --check` pass; the
  unrelated renderer diagnostic remains unstaged.
