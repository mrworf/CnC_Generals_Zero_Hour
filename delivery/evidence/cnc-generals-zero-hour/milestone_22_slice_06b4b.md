# M22 slice 06B4B — source depth bias through public GPU pipeline

Original `DX8Wrapper::Set_DX8_Render_State` publishes only source `D3DRS_ZBIAS=0` and authored decal/water-track `8`, rejecting other values before cache mutation. Original source snapshot retains the selection; `OriginalGpuEdge` translates only its physical raster descriptor. [Microsoft's legacy Z-bias behavior](https://learn.microsoft.com/en-us/previous-versions/windows/desktop/bb324464%28v%3Dvs.85%29) specifies that higher positive bias places coplanar polygons in front, whereas [Vulkan's depth-bias equation](https://registry.khronos.org/vulkan/specs/latest/pdf/vkspec.pdf) adds a positive constant-factor depth offset; SDL_GPU [public rasterizer state](https://wiki.libsdl.org/SDL3/SDL_GPURasterizerState) exposes that factor. Thus source 8 maps to public `-8.0`, and reset 0 maps to canonical `+0.0` (not `-0.0`, which would produce a distinct key); existing SDL backend applies the pipeline field. This is bounded semantic translation, not an adapter geometry shift or private Vulkan call.

RecordingGpuDevice checks actual prepared pipeline descriptors, source snapshot, zero/biased/reset state and invalid 7 without publication. SDL_GPU Vulkan with explicit `VK_LAYER_KHRONOS_validation` draws two overlapping source-issued original-FVF triangles under strict LESS depth: unbiased retains base RGB approximately 64:192:128, source 8 promotes overlay RGB approximately 192:48:32, reset returns to the base descriptor/key. The physical test retains source shader/material selection and uses only the original DX8 state call, not an invented decal pass; the original rigid/skin decal owner render and Flush queue remain required 06B4C/D.

Source inventory: `DX8MeshRendererClass` selects 8→0 for decals; `W3DWaterTracks` also has 8→0; original `SceneClass` and `W3DScene` extra-pass wireframe select 7, but those physical paths remain typed unavailable. Unsupported 7 is explicit, not silently ignored; slices 07/08 must establish retail reachability and extend the bounded profile if required, without weakening acceptance. No retail scene or 06B aggregate claim here.

GCC/Clang Debug full non-LAN **149/149** each, LAN **4/4** each, focused GCC/Clang ASan/UBSan/LSan original-W3D suites **7/7** each, explicit validation-layer GPU **5/5**, source identity/provider-removal and dependency-ledger check pass. Read-only retail symlink unchanged.
