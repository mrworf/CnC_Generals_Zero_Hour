# M22 06C3C2B3B mixed original-source scene evidence

## Owned source and lifetime

The owned W3D packet adds `TEST.SKINHLOD`/skin and `TEST.TWO01` to B3A's rigid and two static levels. `WW3DAssetManager` constructs all render objects; `SimpleSceneClass` queues them; each observed frame enters `WW3D::Begin_Render`, `Render(scene,camera)`, and `End_Render`. The test-only skin material is green and untextured, the decal uses original `DecalSystemClass` with emissive green, and source positions distinguish the left rigid/decal/depth, centered skin, and right translucent-sorted mesh. No adapter triangle or retail fixture is used. The original render/decal/queue path decides geometry, shader, ZBIAS, sorting and draw order.

The public bgfx witness reads both BGRA8 and RGBA8 color targets at 160×120 and 200×150, each in a fresh device generation. One frame contains rigid, HLOD skin, decal, translucent sorting, and both static levels together; source static hooks still drain level 2 then level 1. On a D24S8 attachment, every generation also requires changed RGBA pixels in the expected source-screen region when skin, decal, or sorted mesh is added separately and when an original front mesh changes from in front of to behind the rigid mesh. The outer clear pixel is invariant across those controls and the combined frame. These are absence/inverted-depth negative oracles, not mere draw-count checks. The source pipeline maps depth testing; it does not claim a new original stencil-comparison feature. Existing M30 device stencil probes and original viewport stencil-clear tests remain separate.

Recording asserts the combined and three separate category marker/draw intervals in order, B3A static drain order (level 2 then level 1) and removal control, and rejection of cached-source-buffer retirement during an active source frame. Failed source draw task retirement and fresh-frame retry remain exercised by the B3A source-only test path. All source object refs return to one before release. WW3D shuts down while the edge/device remain alive, retiring source dynamic pools and edge-held buffer refs before the edge destructor; the public device reports only the two caller-owned attachments then zero after their destruction. Four sequential device generations prevent stale-handle reuse.

## Gates

GCC and Clang Debug source-only Recording mixed tests pass. Each Debug compiler passes 30/30 fresh-process mixed physical Vulkan runs with the Khronos validation layer and output scan. Both GCC and Clang physical ASan+UBSan mixed runs pass with LSan disabled because host libdbus leaks are external; leak-capable source-only GCC and Clang ASan+UBSan/LSan tests pass outside the ptrace-limited sandbox. B3B2's original-process service bootstrap precedes device/asset work; B3B3's public readback completion boundary precedes pixel inspection.

The ordinary full suites remain asset-free; opt-in GPU CTest is validation-scanned. GCC/Clang Debug and sanitized full builds pass; each final 181-case GCC Debug, Clang Debug, GCC sanitized and Clang sanitized host asset-free suite passes after the combined-frame fixture. Both Debug suites were rerun on the host after sandbox LAN socket failures. Clang sanitized was configured with `ZH_ENABLE_GPU_TESTS=ON` during prior diagnosis, so its unfiltered 196-case LSan run reported known host libdbus leaks in physical tests; its complete asset-free `-LE '^gpu$'` suite is the leak-capable canonical gate, with physical cases separately run under ASan+UBSan and validation. ABI/provider-removal, dependency-ledger, renderer and source tests are included in the cumulative gate. No retail symlink content is modified.
