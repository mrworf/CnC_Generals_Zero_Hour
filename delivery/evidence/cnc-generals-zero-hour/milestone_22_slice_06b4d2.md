# M22 slice 06B4D2 — original 06B same-Flush aggregate

An owned original W3D rigid mesh, HLOD/HTree bone-updated skin child, and separate rigid delayed object run `MeshClass::Render`/`HLodClass::Render` into a **single** original `DX8MeshRendererClass::Flush`. Original `MaterialPassClass` issues immediate rigid/skin passes and cull-volume APT dynamic indices; the separate rigid override selects the source-delayed pass and suppresses that mesh's decal, while the two original decal owners remain queued on the other meshes. Recording observes at least five category draws including source skin dynamic vertex and cull-volume dynamic index branches, then exactly two source concrete decals bracketed by ZBIAS 8→0, then a delayed draw. Original required texture is nonmissing, source final indices are 16-bit, and no adapter-created geometry/material/pixel or direct decal accessor participates. A later decal draw failure aborts before delayed work, resets source bias, and the original objects re-register/requeue to reproduce the entire pass order without stale owners or texture handles.

The identical source-scheduled aggregate fixture runs on SDL_GPU Vulkan under explicit `VK_LAYER_KHRONOS_validation`, using original camera projection. Two distinct device generations and offscreen target sizes (160×120 and 240×180) read back respectively **153** and **325** nonbackground covered pixels, with bounded target/depth/source teardown and zero Validation Error/VUID. These are original owned W3D fixture pixels, not retail-scene screenshots. Earlier accepted 06B slices supply the negative unsupported stage/texture/material, dynamic APT/index, >65,535 skin batch, disabled/translucent/no-APT/sorted decal, invalid bias and upload/draw/resource routes; this slice supplies their complete interleaved source scheduler and failed later-decal requeue.

GCC/Clang Debug non-LAN **151/151** each, LAN **4/4** each outside UDP sandbox, focused GCC/Clang ASan/UBSan/LSan original-W3D **8/8** each outside ptrace sandbox, explicit validation-layer GPU **7/7**, source identity/provider removal and checked dependency ledger pass. Read-only retail symlink unchanged. The current retail full-draw smoke inventories only three rigid unsorted unskinned child meshes without cull tree; it cannot establish reachability of all 06B families. Slice 08 must audit actual campaign/skirmish encounters and execute every required source route, while 06C sorting/static/WWShade, 07 GameClient integration and 09 final visual/full acceptance remain pending. Thus 06B is accepted only for the owned source-route aggregate, not milestone M22 or retail coverage.
