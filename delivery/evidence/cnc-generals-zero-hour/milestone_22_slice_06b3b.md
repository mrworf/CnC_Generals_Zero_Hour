# M22 slice 06B3B — original rigid delayed additional pass

Original `MeshClass::Render` selects `RINFO_OVERRIDE_ADDITIONAL_PASSES_ONLY`, suppresses its base task and queues the original rigid container's delayed `MatPassTaskClass`. Original `DX8MeshRendererClass::Flush` traverses rigid, skin, decal, then delayed rigid lists. Its original delayed renderer now reaches the physical wrapper; on Linux device failure it leaves the failed original task linked for the caller's scene abort/reset, and unlinks/deletes each successful task. The native branch remains unchanged. No adapter queue or substitute material/draw producer was introduced.

The owned source-W3D Recording scenario queues delayed rigid alongside an original HLOD skin with immediate additional pass, verifies two source skin draws precede the one delayed draw, and proves the subsequent empty delayed queue emits no draw. A terminal scenario injects upload failure and draw failure into a delayed-only frame, sees zero accepted draws, aborts and invalidates original containers, then re-registers the original model and gets exactly one indexed retry draw. Source material opacity is not left modified. Initial placement of this terminal reset in the middle of the broad fixture exposed original rigid append-lock capacity reuse; moving the abort/reset witness to the final scenario boundary kept the original allocator intact. A source-selected cull-volume pass remains typed pending 06B3C.

The SDL_GPU Vulkan `original_w3d_lit_category_gpu` readback sees non-background delayed-only rigid pixels distinct from base rigid and HLOD skin frames under explicit `VK_LAYER_KHRONOS_validation`, zero reported Validation Error/VUID. GCC and Clang debug full non-LAN suites 147/147 each; LAN 4/4 each outside sandbox; focused original W3D/retail sanitizer suites 6/6 each; GPU label 5/5. Identity/provider-removal checks are included in the full suites. Original dependency-ledger hash and `git diff --check` pass. This is owned-fixture delayed behavior only: retail reachability remains slice 08, cull-volume slice 06B3C, and the 06B aggregate is not accepted.
