# M22 slice 01 — original WW3D CPU graph

The source closure is documented in `docs/original-ww3d-cpu-closure.md` and explicitly compiled in `CMakeLists.txt`. The owned RAMFile/ChunkSave fixture in `tests/original_rendering/test_w3d_cpu_graph.cpp` loads through original WW3D asset-manager, mesh, HLOD, hierarchy, raw animation, material/shader, mapper and texture providers; it creates original render objects and releases them. Original `texture.cpp` is the sole `Load_Texture` provider. Physical texture Apply fails explicitly before the slice-03 device edge exists.

Positive witnesses: mesh vertices/triangle, shader destination blend, HLOD model subobject and hierarchy, raw animation, material opacity/screen mapper, texture-stage name and filter state, original prototype teardown. Negative witnesses: duplicate prototype, excessive mesh vertex count, excessive HLOD count, excessive texture/material name, unknown chunk ID, and attempted device Apply. The authored original manager may publish earlier chunks before a later error; retail scenario transaction/retry is assigned to slice 04.

Validation (GCC Debug and Clang Debug): `cmake --build --preset linux-{gcc,clang}-debug -j8 --target original_w3d_cpu_graph_tests` and `ctest --test-dir build/linux-{gcc,clang}-debug --output-on-failure -R '^original_w3d_'`: 6/6 each, including M20 schema identity/provider-removal and M22 original-source identity/provider-removal. Source identity checks both compile inventory and linked map/executable witness. The provider-removal test asserts removing canonical original `texture.cpp` fails identity. This does not claim Vulkan, visual, retail recording, or full milestone acceptance.

The full GCC Debug build succeeded. The 131-test GCC suite had 129 passing tests in the restricted sandbox; only two LAN tests failed because UDP socket creation was not permitted. Those exact two tests passed 2/2 on a permitted local-socket rerun outside the sandbox. No renderer/WW3D test failed.
