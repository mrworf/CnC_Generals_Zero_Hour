# M22 07B1 original 2D status-circle ownership evidence

The original `RTS2DScene` constructor now requires an active `OriginalGpuEdge` on the CPU-only path, then executes its authored `NEW_REF(W3DStatusCircle)` and `Add_Render_Object` operations. Its original destructor removes the object and releases the caller ref on both configurations. Without a physical edge, the typed error still occurs before any status owner or production display slot is published. This does not translate `RTS2DScene::draw`, `W3DStatusCircle::Render`, fades, or `W3DDisplay::init`; physical 2D pixels and production factory promotion remain pending.

The original presentation test constructs two fresh Recording edge/scene generations, verifies the canonical status-circle dynamic type, scene attachment and exactly two refs, then tears each down with zero device resources. It also retains the no-edge negative and checks all production display slots remain unpublished. The source ledger records the changed original scene owner. GCC/Clang Debug and leak-capable sanitizer focused source/identity/provider tests pass. Four native builds and sequential full asset-free suites pass 188/188, including ABI, identity, provider-removal, LAN and clean original-source dependency ledger checks.
