# M22 slice 07E0D3 evidence: original WW3D static-cache retirement

The CPU original `W3DDisplay` destructor now calls `Debug_Statistics::Shutdown_Statistics()` before asset/WW3D teardown, matching the native owner order. CPU `WW3D::Shutdown` asks the `MultiListNodeClass` AutoPool to retire its slabs only after scene/list shutdown. The pool refuses retirement while any node remains live, leaves that node valid, frees all blocks when empty, supports repeated empty retirement and allocates correctly in a second generation. Native Windows behavior and class layout are unchanged.

Allocation trace/control: before D3, the pending original-factory GameClient diagnostic ended at 28 live GameMemory allocations versus 26 for device-only and 22 for the established no-device baseline. Exact surviving source traces were the 10,248-byte `MultiListNodeClass` slab and nine-byte `Record_Texture_End` statistics string. After D3, the same production-order diagnostic ends at 26 after GPU teardown, exactly the device-only control. The +4 device/driver process-scoped allocations are **not** counted as a D3 fix or silently exempted from the M20 absolute-baseline assertion; factory acceptance remains separate. The diagnostic still exits 4 on `26 != 22`, as expected for pending E0E.

On the isolated D3-only tree, fully rebuilt GCC Debug, GCC Release, Clang Release, GCC ASan+UBSan and Clang ASan+UBSan non-GPU suites each pass 194/194, including source/provider/ABI checks and a clean dependency ledger. GCC Debug and Clang Release each pass 30/30 fresh-process host Vulkan display-owner and view-scene repetitions under validation, exercising two display lifetimes and four device generations. The source-only focused presentation, display and view tests pass, and `git diff --check` is clean.

An earlier mixed-worktree run was 190/194 solely because pending D1/E0E source hashes and E0E ABI changes were present. It is diagnostic, not acceptance evidence. Those pending edits and the unrelated renderer diagnostic were isolated recoverably for the clean gate and are restored after this independent commit. Retail symlink content was untouched.
