# M22 06C4B1 complete supplied-retail W3D archive scope evidence

The audit enumerates only the two already configured, user-owned base Generals and Zero Hour roots, with no private root or archive name printed or committed. It includes every BIG recursively, including nested archives, rather than assuming a primary W3D archive, and fails closed if either root is unavailable, duplicate, lacks BIG input, contains loose `.w3d`, or has a malformed archive. An owned two-root fixture puts SHDMESH only in a nested secondary BIG and proves detection; a loose W3D negative and duplicate-root negative prove scope closure. The checker also now matches the original BIG parser's treatment of zero-length entries whose offset precedes the table end; a separate owned positive protects that source-compatible case while positive-length out-of-range entries still reject.

The read-only supplied-retail aggregate covers 51 BIG archives (35 top-level and 16 nested) and no loose W3D files. It finds 13,562 `.w3d` entries and 132,287 bounded top-level chunks, with zero SHDMESH. Five opaque entries are the already-classified Zero Hour cases; all five reject in fresh original W3D loader processes, with zero accepted, unresolved or SHDMESH valid-prefix/malformed-header candidates. The combined source-qualified result is `source_wwshade_required=no` for these supplied roots. This does not prove other distributions, mods or future content; the authored enabled WWShade branch still needs a guarded compile/negative witness, and the complete owned mixed WW3D frame and C4 acceptance remain pending.

All archive bytes remained read-only through file mapping and bounded in-memory loader stdin; no retail file, symlink, name, path, hash or payload was changed or committed. No product renderer behavior changed. Focused GCC/Clang Debug and sanitized controls pass. All four final native builds and sequential full asset-free host suites pass 185/185, including original source identity, ABI, provider-removal and dependency-ledger checks. The sanitized source-only controls remain leak-capable; this read-only audit makes no physical GPU claim.
