# M22 slice 06A3C: source-owned lit category and rigid aggregate

The owned W3D fixtures load through the original `WW3DAssetManager` and original `MeshClass::Render`, register through the original mesh renderer before its first physical flush, and issue original `DX8TextureCategoryClass` light selection, world transform, material/mapper/alpha state and polygon indexed draw. Distinct original W3D normal-bearing N0, N1+UV1 and N2+UV2 fixtures select three exact lit vertex variants and physical 0/1/2-stage shaders. Recording asserts the ordered original light→world→variant→draw command chain, including directional, point and bounded four-light selection. The original null-light branch retains global light state; it is not a fabricated rejection.

An actual SDL_GPU Vulkan render target receives category-issued pixels from original W3D/TGA producers under explicit `VK_LAYER_KHRONOS_validation`. Ambient-only and directional pixels differ; a point outside the original ten-unit range reproduces ambient-only pixels, while an in-range point differs from both. Four authored lights, alpha override, and normal-bearing 0/1/2-stage meshes render non-clear source frames. The pixel fixture sets source light ambient to zero explicitly: original `LightClass` defaults ambient to white and would otherwise saturate the material, obscuring valid differences. Renderer attachment resize/recreation from 160×120 to 240×160 succeeds with live original mesh/category owners, then clears the original queue/assets and destroys GPU attachments/device before `SDL_Quit`. Prior 06A3B2 separately proves two fresh physical device generations and exact lit per-vertex equations; this slice does not claim retail scenes or display integration.

Recording rejects invalid original source ambient without drawing, then requeues and succeeds after reset; injected GPU upload failure draws nothing, then original source requeue succeeds. Existing original shader-source tests reject missing required source environment, unsupported normal-free lit FVF, invalid slots/type and resource failures; they preserve authored null-environment retention where applicable. Full rigid 06A1/A2/06A3 aggregate remains green. No adapter-owned geometry/material, original-game mutation, generated scene substitution, or original source change was made.

Validation: GCC and Clang Debug complete builds, 146/146 non-LAN and 4/4 LAN tests each (including provider-removal/source-identity/ABI/link); GCC and Clang focused ASan/UBSan/LSan 5/5 each; all five Vulkan GPU tests pass with zero Validation Error/VUID; `git diff --check` clean. Dependency ledger source hashes remain unchanged because only fixture/CMake/evidence files changed. 06B/06C, GameClient integration 07, retail recording 08 and final validation/visual 09 remain mandatory.
