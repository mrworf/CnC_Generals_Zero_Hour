# M22 slice 06B4A — canonical original decal CPU producers

Production `zh_w3d` links the original `matrix4.cpp`, `matrixmapper.cpp`, `projector.cpp`, `decalsys.cpp` and `decalmsh.cpp` in its single `ZH_WW3D_CPU_ONLY` ABI configuration. Original `MeshClass::Create_Decal` drives rigid local APT/clipping/UV and HLOD/HTree bone-updated skin world APT/UV through the original `DecalSystemClass` and concrete `RigidDecalMeshClass`/`SkinDecalMeshClass`; no adapter supplies mesh/material/texture geometry. SDL_GPU's public rasterizer supports depth bias, so the original unavailable-bias polygon-shift branch is not selected on Linux; actual source `D3DRS_ZBIAS=8`/0 physical translation is explicitly pending 06B4B.

The owned original-W3D fixture verifies the source generator's ref-owned default optional null material/texture, disabled decals, translucent default rejection, distant projector empty APT and corrupt polygon rejection before original APT dereference. After enabling the original translucent selector, the generator lists precisely the original rigid mesh and bone-updated skinned mesh. `MeshClass::Render` and original `DX8MeshRendererClass::Flush` execute a real original base indexed draw, then throw the explicit typed `original decal mesh pass requires GPU translation` at the physical queue slot; no physical decal is accepted in this slice. Failure cleanup invalidates the original queued links before owner release; deleting both authored IDs and re-locking the same original system produces a new source decal ID and rigid owner without a stale link. Caller-owned Recording target/depth textures and source texture owners return to zero. Full physical decal, depth-bias, source order, visual pixels and retail routes remain mandatory 06B4B–D/08/09.

Source identity/link-map requires all five new canonical providers and their independent removal fails at both compile and link checks. Original dependency-ledger SHA/source-family entries account for the new graph and modified mesh/renderer methods. GCC/Clang Debug full non-LAN suites **149/149** each; LAN **4/4** each; GCC/Clang focused ASan/UBSan/LSan original-W3D/retail suites **7/7** each; explicit validation-layer Vulkan GPU suite **5/5**, no Validation Error/VUID. Ledger checker and `git diff --check` pass. Retail symlink read-only and unchanged. This is not 06B or milestone acceptance.
