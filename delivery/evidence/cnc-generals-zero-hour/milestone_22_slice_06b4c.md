# M22 slice 06B4C — original concrete rigid/skinned decal physical draw

The original `RigidDecalMeshClass::Render` and `SkinDecalMeshClass::Render` execute from decals generated on original loader-owned rigid and HLOD/HTree bone-updated skinned meshes. Their original dynamic XYZNDUV2 vertex/index locks, source material/shader/optional null texture runs, rigid parent-world and skin world-identity transforms, 16-bit indexed `DX8Wrapper::Draw_Triangles` and `OriginalGpuEdge` physical submission are intact. A layout-neutral CPU-configured `MeshClass::Peek_Decal_Mesh` exposes the existing owner only to the direct boundary witness; it is never a substitute scene path. Original decal creation preflights conservative 16-bit clipped-geometry capacity before publishing source arrays, and concrete draw preflights source indices/counts before physical state or uploads. The original `DX8MeshRendererClass::Render_Decal_Meshes` remains typed pending 06B4D, as does the source `ZBIAS=8`/0 queue bracket.

The Recording witness observes exactly two original-owned indexed draws with source dynamic offsets, 16-bit last accepted index bytes `0,1,2`, rigid world translation followed by skinned world identity, and no adapter-created geometry. Disabled decals and source-sorted skin skip without a device session. Another original mesh with a source material/shader requiring an absent texture fails at its generation-bound texture stage, releases owner and succeeds on the independent valid source owner. Injected dynamic buffer upload failure aborts before draw; injected second-draw failure records exactly the first owner draw; fresh original edge sessions retry the same two owners and release all caller-owned targets/depth and GPU resources.

With `VK_INSTANCE_LAYERS=VK_LAYER_KHRONOS_validation`, SDL_GPU Vulkan separately renders each original concrete owner and reads back 153 covered pixels for the rigid and 153 for the skin on 160×120 offscreen targets. No Validation Error/VUID; the visual witness is per owner but **not** an original renderer queue or retail-scene frame. The 06B4D aggregate must execute the canonical Flush slot and ZBIAS bracket (including rollback/requeue), while 08/09 own real retail scenes. Optional authored null material remains valid and required source texture failure is explicit; no generated placeholder pixels are accepted.

GCC/Clang Debug full non-LAN **150/150** each, LAN **4/4** each; GCC/Clang focused ASan/UBSan/LSan original-W3D/retail suites **8/8** each; explicit validation-layer GPU **6/6**, source identity/provider-removal and dependency ledger pass. Read-only retail symlink unchanged. No aggregate 06B or milestone acceptance.
