# M22 06C4C guarded enabled WWShade branch evidence

The test reads each preset's canonical `zh_w3d` compile commands for original `assetmgr.cpp` and `ww3d.cpp`. Production commands remain CPU-only and `USE_WWSHADE`-disabled; their built objects have no unresolved `SHD_Register_Loader` or `SHD_Flush` call. The test adds only `-DUSE_WWSHADE=1`, compiles those exact source bodies to temporary objects, and verifies original `SHD_Register_Loader` and `SHD_Flush` relocations respectively. GCC/Clang Debug and both sanitized compile/symbol witnesses pass. The independent authored macro-expansion test also passes, including the disabled no-op and enabled call forms.

No production define, object layout, shader, loader, runtime behavior, retail asset, or original source file was changed. This is guarded compile/symbol evidence only, not an enabled link/runtime or visual shader-family claim. The supplied-retail audit found no source-required SHDMESH across the two configured roots; other content could differ. C4 still requires the complete owned rigid/skin/decal/immediate/delayed/cull/static/translucent/sort frame, negative recovery and physical Vulkan evidence. All four native builds and sequential full asset-free host suites pass 186/186, including source identity, ABI, provider-removal and dependency-ledger checks. Sanitized source-only controls remain leak-capable.
