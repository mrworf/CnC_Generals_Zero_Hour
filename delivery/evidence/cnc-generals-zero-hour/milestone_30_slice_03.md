# M30 slice 03 — physical shader/draw evidence

Status: focused public-bgfx shader, binding, state and draw acceptance on RTX/Vulkan. Window presentation, four-preset/full-suite and original retail scene acceptance remain pending.

- Parent slice: `ba4e34305435b8ff0af2ebd7665d71831641d368`. The backend retains opaque engine handles; no private Vulkan path was added.
- Physical command trace: full red pass clear → green acceptance draw → camera viewport → blue inset clear → green post-clear draw. Readback proves outer/inset/draw pixels in source-shaped order. Stale generations, uninitialized LOAD and exhausted views remain rejected.
- The original source's `D3DRS_STENCILWRITEMASK 0x80` requirement is represented by public `bgfx::setStencil` with the pinned Vulkan backend's back-word write mask. A clear-to-7, MSB-only replace-to-0x80, compare-equal-0x87 draw produces green only where low bits survived; no private API or §9 exception is needed.
- M29's generated Vulkan shader containers are offline-compile evidence, not physical-binding evidence. A physical `renderer/ui.vert` load revealed a length-prefixed reflected field `ZhStageUniforms.frame_data.viewport`: the leading byte is field length, not part of the name. Public bgfx rejects the interior dots when creating uniform handles. M30 normalizes only interior `.` to `_` in a copied envelope's bounded reflected-name fields, rejects unknown characters, malformed counts and duplicate normalized names, and preserves SPIR-V bytes plus M29 binaries/manifests. A wrong-stage manifest also rejects.
- All 39 pinned shader binaries create/destroy through the public bgfx Vulkan runtime. A video triangle proves the normalized frame viewport uniform and source-binding-zero sampled texture drive green pixels; missing uniform or texture bindings reject. An original-FVF XYZ/diffuse/UV 16-bit indexed draw with nonzero base vertex produces expected pixels; out-of-range indices reject. A source-alpha texture blend produces approximately half-red/half-green pixels. The point shader with frame/object/material uniform blocks, texture sampling and integer size 7 produces a green center point; fractional point size rejects. The source-shaped post-clear/stencil tests prove draw ordering and D24S8 behavior.
- `cmake --build --preset linux-gcc-debug --target renderer_bgfx_device_tests renderer_contract_tests renderer_sdl_gpu_device_tests -j4`: pass. Focused `ctest` regex `renderer_(contract|sdl_gpu_device|bgfx_device_contract)$`: 3/3 pass. `env VK_INSTANCE_LAYERS=VK_LAYER_KHRONOS_validation build/linux-gcc-debug/renderer_bgfx_device_tests --gpu`: pass on NVIDIA GeForce RTX 4070 / Vulkan 1.4.341 / driver 610.57.04 with Khronos layer 1.4.357, exit 0 and no emitted validation errors/VUIDs. Host ICD access required escalation because the sandbox hides NVIDIA devices.

The shader family check proves physical creation and reflection closure, not pixels for every family. M22 owns actual original WW3D scene-family/retail pixels; M30 slice 04 still owns SDL3 presentation, lifecycle, all presets, full suite, sanitizers and evidence comparison. No retail content was copied, hashed or modified.
