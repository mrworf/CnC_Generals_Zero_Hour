# M22 06C3C2B3B1 original dynamic pool growth evidence

Linux original dynamic DX8 and sorting VB/IB pools now release only the pool's old-generation reference during growth. A submitted edge snapshot or another external owner keeps the old buffer and bytes valid until its own release. Existing in-use, 16-bit-bound and deinit-with-live-owners guards remain. Class layout and Windows branches are unchanged. The B3B source fixture no longer lowers the original sorting minimum as a workaround.

`original_w3d_dynamic_growth` passed GCC/Clang Debug and Release, and GCC/Clang ASan+UBSan with leak detection on the host. It covers all four VB/IB × DX8/sorting growth paths, distinct new and retained old buffers, unchanged old source bytes, Recording edge cache ownership of DX8 buffers, rejected simultaneous access, rejected deinit while live, and zero final source/public resources. Both sanitized toolchains also passed the four-generation B3B mixed source Vulkan diagnostic at default sorting capacity with validation enabled; B3B's unsanitized repeated physical acceptance remains pending and is not claimed here.

All four GCC/Clang Debug/Release full builds passed. Canonical asset-free CTest passed 177/180 in each preset. Only the three original resources/data/process source-identity ledger checks failed, because separately pending B3B `OriginalGpuEdge` and `ww3d.cpp` shutdown edits were present in the shared working tree; this slice's VB/IB ledger hashes and witness were updated. B3B must clear the remaining ledger drift before acceptance. `git diff --check` passed.
