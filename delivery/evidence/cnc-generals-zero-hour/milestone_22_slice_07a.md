# M22 07A canonical status-circle compile-frontier evidence

The authored `W3DStatusCircle.cpp` now compiles in the guarded full-draw object target under the same `ZH_WW3D_CPU_ONLY` layout as original GameClient sources. Linux portability changes are limited to `PreRTS.h` include order, case-correct canonical headers and equivalent constructor spelling accepted by GCC and Clang. The CPU compatibility header gains source D3D blend-operation tokens; their values match Microsoft's [render-state enumeration](https://learn.microsoft.com/en-us/windows/win32/direct3d9/d3drenderstatetype) and [blend-operation enumeration](https://learn.microsoft.com/en-us/windows/win32/direct3d9/d3dblendop). The physical mapping of subtraction/multiply fade states remains 07B work.

The new compile/link CTest requires exactly one canonical full-draw source command, its original constructor/`initData`/`Render` definitions, and its object in the full probe link map. It rejects accidental physical status-circle symbols in schema-only production. The existing original presentation test still requires `RTS2DScene` to reject a missing physical device before scene/asset publication. Thus this slice proves source availability, not original 2D scene creation or pixels; no production factory switch, retail change or unsupported runtime no-op is claimed.

GCC/Clang Debug and both sanitizer preset focused source, identity, provider-removal and typed no-device tests pass. Source-only sanitizer controls run with leak detection active. The dependency ledger records both the canonical new source object and the updated CPU wrapper header; its direct check passes. All four native builds and sequential full asset-free suites pass 188/188, including original source identity, ABI, provider removal, LAN and ledger checks. 07B must still prove physical construction, draw, pixels and failure/reset lifecycle before any full 07 claim.
