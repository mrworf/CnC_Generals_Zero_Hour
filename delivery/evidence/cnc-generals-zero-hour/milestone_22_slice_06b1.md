# M22 slice 06B1: original dynamic vertex/index buffer ownership

The canonical original `dx8vertexbuffer.cpp`/`dx8indexbuffer.cpp` now execute `DynamicVBAccessClass`/`DynamicIBAccessClass` pool allocation, 5,000-element recycling, offset/wrap/growth, lock, reset and reference ownership in their Linux CPU configurations. Only the physical D3D allocation/lock is replaced with the original buffer owner's CPU upload bytes; the Windows source path and one canonical FVF identity are preserved. Original `DX8Wrapper` dynamic overloads bind those original owners, preserve both source access offsets and original index base in its indexed physical command, and reject declared-access overrun before upload/draw. Sorting-access CPU ownership is retained, while its physical draw remains typed unavailable until 06C.

Direct original-owner Recording witness proves first and second access offsets 0/3, exact `VertexFormatXYZNDUV2` and 16-bit index bytes at those positions in uploaded device buffers, wrap at the 5,000-element bound, growth to 5,001, double-acquire and live-owner teardown rejection, valid sorting source locks with a typed sorting device stop, and bounded no-draw on invalid index/vertex/base ranges. An injected vertex upload failure draws nothing and retries from the same source-owned buffers without stale alias. The Vulkan source-buffer upload runs under explicit Khronos validation, stops at the unfinished material/skin graph instead of claiming a fabricated skinned frame, and tears down device/attachments before SDL video shutdown. 06B2 must lower source FVF `0x252` and execute original skin category drawing; 06B3/B4 remain required for additional passes and decals.

Both GCC/Clang full Debug builds pass 146/146 non-LAN and 4/4 LAN tests each (source identity, provider removal, ABI/link included). Focused GCC/Clang ASan/UBSan/LSan source graph, source shader, original first edge, injected failure and retail draw tests pass 5/5 each; all five Vulkan GPU tests pass with zero Validation Error/VUID. `tools/check_original_dependency_ledger.py` validates updated original-source hashes and newly deferred skin/additional/decal/sorting entries. Original-game symlink remains read-only and untouched. No positive skin or aggregate 06B acceptance is claimed.
