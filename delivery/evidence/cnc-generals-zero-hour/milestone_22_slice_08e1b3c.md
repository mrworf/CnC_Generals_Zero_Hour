# M22 08E1B3C: generated stream-to-buffer evidence

The accepted original stream registry and portable VideoBuffer base compose a
generated bounded frame transaction: open, ready, decompress, render, advance,
end and close. The two-generation probe rejects unknown/duplicate open,
invalid buffer extent, render before decompress and injected render failure,
then proves reset/retry, provider removal and zero ownership. No media decoder,
retail byte, W3D texture or pixel result is claimed.

- Focused GCC/Clang provider/removal checks: **2/2** each.
- Fresh non-GPU/non-LAN suites: **223/223** in all six configurations.
- Host leak focus: **2/2** GCC and Clang; Vulkan **1/1**; LAN **4/4** in all six.
- Direct/registered ledger and `git diff --check` pass; unrelated renderer
  diagnostic remains unstaged.
