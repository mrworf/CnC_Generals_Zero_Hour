# M22 slice 06C3B3B — original WW3D source frame lifecycle

One canonical `WW3D::Begin_Render`/`End_Render` source body now runs in both native and Linux CPU configurations. The original ordering remains: initialization/device guard; memory counters; canonical `TextureLoader::Update(network_callback)`; dynamic VB/IB frame reset; statistics; capture gate; IsRendering; full-target viewport and source clear; physical scene begin. End drains the original `SortingRendererClass`, ends the physical scene and optionally presents, increments WW3D frame count, ends statistics, ends snapshot, and invalidates selected state. CPU-only guards reject unsupported capture and reentry; source clear packs through original `DX8Wrapper::Convert_Color` to authored eight-bit ARGB before public pass translation. The native method and path remain source-owned.

The GPU edge only binds *caller-owned* color/depth handles and extent, translates source-selected independent CLEAR/LOAD plus quantized RGBA/depth and full-target viewport to a public pass, and ends/presents on source command. It creates no render target, geometry, material, pass schedule, or callback. Because SDL_GPU opens a pass before setting its viewport, the original source's full-target selection is carried into pass-open and then physically applied, without moving WW3D's update/clear/BeginScene decision order. Failed begin/viewport/end and stale handle paths clear the source rendering flag and release an owned active pass where possible; the source sorting queue retains its prior exception cleanup.

An owned original asset-manager/full-`WW3D::Init` fixture calls the original Begin/End methods directly, not a preopened public pass. Recording checks no-init semantics; missing attachment, test-only negative capture injection (excluded from production ABI), invalid source clear, reentrant Begin, inactive End, destroyed/stale depth, stolen physical pass at End, FrameCount failure rollback, retry after physical replacement, independent color/depth load modes, source quantized clear, no empty-queue network callback, original disabled memory counters, and bounded teardown. Its only explicit wrapper VIEW/WORLD transform selections are original DX8 state calls needed by the original sorter flush; no scene or category draw is claimed.

An original `WW3D::Begin_Render(true,true,...)`/`End_Render(false)` Vulkan witness executes at **160×120** and **240×180** on distinct `SdlGpuDevice` generations with explicit `VK_LAYER_KHRONOS_validation`; readback verifies every pixel RGBA **(51,102,154,196)**, including authored float-to-byte quantization. The validation wrapper rejects any Validation Error/VUID. GCC/Clang Debug full non-LAN **162/162 each**, LAN **4/4 each** sequentially, focused source/init/texture ASan+UBSan+LSan **3/3 each**, cumulative GPU **9/9**, original CPU/provider-removal/ABI identities, dependency ledger, and diff whitespace pass. A concurrent first GCC/Clang full run had an unrelated `original_data_map_metadata` abort on GCC; its isolated rerun and a subsequent standalone GCC full **162/162** both passed. Private retail content was untouched.

This proves only the original full-target frame lifecycle. `WW3D::Render(Scene*)` camera viewport/partial clear, original object/static-sort aggregate and retry remain 06C3C; WWShade remains 06C4; production `W3DDisplay` 07; retail recordings 08; full retail Vulkan/visual acceptance 09.
