# M22 slice 07E0E evidence: production-order original empty factory

The full-presentation probe has an explicit opt-in profile that creates a public bgfx device, `OriginalGpuEdge`, and owned color/depth targets before real `GameMain`. `LinuxGameClient` then creates exactly one canonical `W3DDisplay`, `W3DView`, and empty `W3DTerrainVisual`; the default runtime keeps the headless factories. A bounded real GameClient update reaches original display draw, and host readback proves an opaque black empty source frame. Factory-attached rigid pixels remain separately planned as 07E0F.

After `GameMain` returns, the test proves `TheGameEngine`, subsystem list, display/terrain globals and every W3DDisplay static owner are gone before retiring two stale non-owning aliases (`TheGameClient`, `TheTacticalView`). Original source ownership shows no allocation delta: both equivalent original-factory and device-only fresh processes have an identical GameMain delta of five and identical post-device residual 26. The established no-device baseline is 22, so the four bgfx/device process-scoped allocations are explicitly measured; the default M20 absolute-baseline assertion is unchanged. Forced device failure, conflicting profiles, and forced partial terrain-factory failure all exit at the process boundary with no published owners and the expected device/no-device residual.

On the exact E0E tree, fully rebuilt GCC Debug, GCC Release, Clang Release, GCC ASan+UBSan and Clang ASan+UBSan non-GPU suites each pass 194/194, including dependency ledger, source/provider and material-ABI isolation checks. The source-local CPU header definition avoids a target-wide ABI flag. GCC Debug and Clang Release each pass 30/30 fresh-process host Vulkan paired bootstrap/rollback repetitions with validation enabled. `git diff --check` and the ledger check pass. The unrelated renderer diagnostic was isolated and retail symlink content was untouched.
