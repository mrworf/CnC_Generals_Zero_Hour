# M22 07C3 original W3DDisplay source-owner lifecycle evidence

The original `W3DDisplay` class now has a bounded Linux CPU owner lifecycle. Its constructor initializes the source-owned slots without publishing them; `init()` requires a live `OriginalGpuEdge`, constructs original `RTS3DInterfaceScene`, `RTS2DScene`, `RTS3DScene`, and `W3DAssetManager`, starts `WW3D`, and publishes all four slots only on success. Re-entry preserves pointer identity. `reset()` follows the source scene iterator to detach objects; destruction releases scene refs and assets and shuts down `WW3D` before the edge ends. Other virtual physical display methods reject with `OriginalW3DDeviceUnavailable`; this does not activate production factory/draw, terrain, shroud, tracks, shadows, particles, or retail loading.

The initialized GameClient source probe verifies missing-edge init failure and same-object retry, reset-before-init rejection, duplicate owner rejection without slot damage, typed draw rejection, stable re-entry, generated original rigid W3D packet loading through the published asset manager, two independently owned mesh refs detached by reset, repeated reset, null slots and zero resources after destruction. Two Recording and two public bgfx Vulkan device generations pass. The host Vulkan test rejects child validation diagnostics under explicit `VK_LAYER_KHRONOS_validation`; GCC and Clang Release each pass 30/30 fresh processes.

GCC Debug/Release, Clang Release, GCC ASan+UBSan and Clang ASan+UBSan full host asset-free suites pass 193/193 each with clean original dependency ledgers, source identity/provider checks, and leak-capable source-only sanitizer controls. Sanitized driver-process leak reports are not claimed as source leak evidence. The unrelated renderer diagnostic and retail symlink contents remain untouched.
