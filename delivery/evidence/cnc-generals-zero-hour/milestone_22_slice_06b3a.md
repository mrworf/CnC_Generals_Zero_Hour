# M22 slice 06B3A — original immediate additional material pass

Original `MaterialPassClass::Install_Materials` now installs its selected material, shader and stages 0–1 through the original `DX8Wrapper`; a nonnull source stage 2–7 fails before source-state mutation. An authored null material retains the wrapper's original default. The original `MeshClass::Render_Material_Pass` executes its skin and rigid non-cull branches, light/world selection, polygon renderer iteration and shared alpha/emissive override restoration; on an injected device error it also restores those shared original values before propagating the failure. Source-selected cull-volume APT remains a typed device-edge stop until 06B3C, and delayed scheduling remains 06B3B. No adapter-owned pass, material, geometry or draw queue was introduced.

`original_w3d_cpu_graph --device-edge` runs original-owned W3D rigid and HLOD/HTree skin objects through original immediate `RenderInfoClass::Push_Material_Pass`, base→additional physical draw order and a source-lit untextured material-pass shader (0 bound texture stages), 16-bit indices, material install and original source transform/light decisions. Negatives: nonnull stage 2 rejected without mutation, injected second draw after successful base fails while shared opacity/emissive remain unchanged, aborted frame requeues the original model/category for two successful draws, and source-selected cull-volume APT reaches a distinct typed stop. Original null material with and without nondefault overrides executes the guarded default path. The owned-fixture 0-stage shader is the reached pass profile here; this is **not** evidence that stage-1/2 additional-pass combinations occur in retail, which slice 08 audits. The existing base-category fixture covers physical 0/1/2-stage variants separately. The missing/required retail-resource scenario and reset/retry remain slice 08.

`original_w3d_lit_category_gpu` on SDL_GPU Vulkan, explicitly `VK_INSTANCE_LAYERS=VK_LAYER_KHRONOS_validation`, reads back changed pixels from both original rigid and HLOD skin immediate additional draws versus their respective base frames. Full GPU label suite 5/5, zero reported Validation Error/VUID. GCC and Clang debug full non-LAN 147/147 each; LAN 4/4 each outside sandbox (POSIX UDP); GCC and Clang sanitizer focused original W3D/retail suites 6/6 each; original source identity/provider-removal in full suites, dependency-ledger checker and `git diff --check` pass. The first concurrent debug-suite run coincided with a dependency-ledger write and saw three transient ledger checks fail; both complete suites were rerun on stable files and passed. Retail scene execution is still the prior smoke category scenario, not 06B3A pass reachability or milestone acceptance.
