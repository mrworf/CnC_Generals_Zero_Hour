# M22 slice 06B4D1 — original Flush decal queue and rollback

The original `MeshClass::Render` schedules original rigid and HLOD/HTree skinned objects and their two source-owned decal meshes. The original `DX8MeshRendererClass::Flush` executes both base categories before `Render_Decal_Meshes`, which now calls the original concrete decal `Render()` overrides between original `D3DRS_ZBIAS=8` and `0` commands. On Linux physical failure, the original queue drops its visible links, restores source bias 0 and propagates the error; the caller explicitly abandons the partial frame. Native source order is unchanged. This uses neither a test accessor nor an adapter-owned scene or geometry.

The owned W3D recording fixture checks base/base→ZBIAS8→decal/decal→ZBIAS0, no draw after reset, and no retained source texture owner. Injected failure on the first and second decal draw separately records an aborted bracket/reset; `Invalidate` clears original category/decal links, and the same original mesh/HLOD owners rebuild and draw both decals on fresh original physical sessions. Source generator lifetime/retry and bounded GPU resource destruction are exercised. This is not the complete same-Flush material/cull/delayed frame, Vulkan queue pixel evidence, retail scene, or 06B acceptance; those remain 06B4D2/08/09.

GCC and Clang Debug full non-LAN **150/150** each (including source identity and provider removal), LAN **4/4** each outside the UDP-restricted sandbox, focused GCC/Clang ASan/UBSan/LSan original-W3D suites **7/7** each outside ptrace-restricted sandbox, explicit `VK_LAYER_KHRONOS_validation` GPU **6/6** with zero Validation Error/VUID, and checked original dependency ledger pass. Read-only retail symlink unchanged.
