# M22 slice 06C3B2 — public full-target clear/load/depth semantics

`RenderPassDesc` now expresses independently selected color and depth CLEAR/LOAD, RGBA clear including destination alpha, and depth clear. Its defaults retain the prior `(0.02,0.02,0.04,1)` color and `1` depth behavior exactly for existing M14/M20/M21/M22 callers. Recording and SDL_GPU reject an unsupported load, nonfinite/out-of-range clear component, uninitialized target LOAD, destroyed/recreated or wrong-format target, midpass begin, and midpass/uninitialized presentation before mutating pass ownership. A completed pass or complete target upload initializes that exact live texture generation; LOAD preserves its prior contents and SDL_GPU disables texture cycling for LOAD in accordance with the installed SDL3 header. Failed pass begin leaves no active pass. Texture destruction during an SDL_GPU pass is rejected, matching Recording.

The public Recording witness checks initial color/depth LOAD rejection, distinct source RGBA/depth values and independent LOAD decisions in the command stream, active-pass and presentation negatives, and rejection after handle generation changes. Direct SDL_GPU Vulkan physical evidence, not an original-frame claim, uses the same GPU device contract at **160×120** and **240×180** on two separate device generations under explicit `VK_LAYER_KHRONOS_validation`: an RGBA `(0.125,0.25,0.375,0.5)` clear yields the expected color and alpha bytes; depth `0.25` occludes z=`0.5` geometry through a subsequent depth LOAD, while clearing only depth to `1` reveals that geometry without clearing the color. Both stale and freshly recreated color handles fail LOAD, and all owned test targets are destroyed. Source-camera partial clear and WW3D Init→Begin/End remain B3/C3C and were not substituted with this direct public-edge fixture.

GCC/Clang Debug full non-LAN **158/158** each; LAN **4/4** each after serial rerun (initial concurrent GCC/Clang LAN runs collided on shared UDP ports); focused renderer/original GCC/Clang ASan/UBSan/LSan **5/5** each; explicit validation-layer Vulkan cumulative GPU **8/8**; `git diff --check` and original dependency ledger pass. The private retail symlink was not modified. B3 must connect exact original source full-target clear and frame lifecycle, C3C camera-viewport clear/scene order, 07 production display, 08 retail recordings, and 09 retail Vulkan/visual/full acceptance.
