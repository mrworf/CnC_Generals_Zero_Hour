# M22 06C3C2B3C original scene fault and C3 aggregate evidence

## Source transaction

The new focused `original_w3d_source_mixed_fault_matrix` mode uses B3B's owned W3D packet and original `WW3DAssetManager` → `SimpleSceneClass` → `WW3D::Begin_Render`/`Render`/`End_Render` path. An invalid authored clear input fails before a public pass; a destroyed bound depth target rejects source Begin. A fresh target is created and rebound, and the same original rigid scene frame then succeeds. `RecordingGpuDevice::fail_draw_after(1)` accepts the rigid draw and rejects the next source draw in three separate frames: HLOD skin category, static-list drain with both levels, and translucent sorting. Each site has a source marker and injected-draw error, no successful frame or active pass after the exception, selected DX8 buffers cleared, scene/object refs back at owning baselines, and a successful same-device retry. The static retry drains level 2 before level 1. The accepted B3A failed-draw task-retirement control remains independent.

`original_w3d_source_mixed_fault_retry_bgfx` runs the same mixed scene on public bgfx/Vulkan at BGRA8 and RGBA8, 160×120 and 200×150, four sequential device generations. In each generation it destroys the bound depth target after a known-good pixel frame, observes rejection without source frame completion, creates and rebinds a replacement, then requires byte-for-byte identical RGBA readback. B3B's combined-category, separate category-region, inverted front/back depth and outer-clear controls remain mandatory afterward. WW3D shuts down while the edge/device still live; only the two caller attachments remain, then zero public resources. The source-only mode also ends with zero Recording resources. No retail symlink content or product source is modified.

## Gates

GCC and Clang Debug source-only focused matrix pass. Each passes 30/30 fresh-process validation-enabled physical retry runs. GCC/Clang leak-capable source-only ASan+UBSan/LSan and physical ASan+UBSan with known external host libdbus LSan disabled pass. GCC Debug, Clang Debug, GCC sanitized and Clang sanitized full host asset-free suites each pass 182/182, including original-rendering/renderer/ABI/provider-removal and clean dependency-ledger checks. The Clang sanitized configuration also registers opt-in GPU tests; its full leak-capable suite excludes `gpu`-labelled physical cases, which ran separately under validation and ASan+UBSan. The sandbox's ptrace restriction cannot run LSan, and LAN socket checks ran on the host.
