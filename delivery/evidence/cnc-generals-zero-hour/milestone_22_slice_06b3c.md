# M22 slice 06B3C — original cull-volume APT/dynamic index; 06B3 owned-route aggregate

Original `MeshClass::Render_Material_Pass` now executes its authored cull-volume branch: inverse mesh transform and local OBB/view vector, original `MeshGeometryClass::Generate_Rigid_APT` cull-tree or no-tree selector, `DynamicIBAccessClass` lock and triangle bytes, original minimum/maximum vertex offset, `MaterialPassClass::Install_Materials`, source world transform and indexed draw. The original `IndexBufferExceptionFunc` hook is defined in its canonical CPU-configured index-buffer source, while Linux C++ errors propagate instead of following the native Windows-kernel exception-swallowing workaround. Invalid source polygon indices are rejected *before* APT dereferences them; selected APT/index/vertex counts beyond 16-bit bounds reject before conversion or physical submission. The source-selected sorting dynamic-index route rejects distinctly pending 06C. Neither APT nor mesh/material selection moved into an adapter.

Owned W3D fixtures load through original `WW3DAssetManager` with (a) no cull tree and (b) an original loader-generated AABTree from the W3D collision flag. For (a), relocating the OBB alone still draws because the original no-tree fallback uses view-facing APT; reversing its basis produces an empty APT and no additional draw. For (b), covered OBB selects a polygon and distant OBB rejects it. Recording verifies base→additional ordering, source dynamic index offset, 16-bit count and **actual last accepted draw index bytes** equal to the original polygon's I/J/K. A malformed original polygon index, source-selected sorted route, injected APT upload after the base physical uploads, and injected second-draw rejection each abort the partial frame; original renderer invalidate/re-register/re-render restores two draws. Empty APT emits only the base. Both cull-tree/no-tree covered/rejected original draw pixels are read back on SDL_GPU Vulkan with `VK_INSTANCE_LAYERS=VK_LAYER_KHRONOS_validation`; no Validation Error/VUID reported.

Read-only retail full-draw smoke's selected original model currently inventories three mesh children, `cull-tree=0 no-tree=3 skin=0 sorted=0` and FVF 274; no private asset bytes or paths are emitted. This bounds *only that smoke scene*, not campaign/skirmish reachability. Slice 08 must still inventory/execute every required retail family, required-resource rejection/reset/retry, and may reveal additional paths. Aggregate 06B3A/B/C closes owned source immediate skin/rigid, delayed rigid and cull/no-cull dynamic index routes; source-authored optional null/default material remains positive and nonnull unsupported stages 2–7 fail. The exercised additional pass is zero-bound-texture shader; the base categories exercise 0/1/2 stages, not a claim of a retail two-stage additional pass. 06B4 decal, 06C sorting/full category interleave and 07–09 remain mandatory; no milestone acceptance is claimed.

GCC/Clang Debug full non-LAN suites 147/147 each, LAN 4/4 each outside sandbox; GCC/Clang focused ASan/UBSan/LSan original-W3D/retail suites 6/6 each; explicit validation-layer Vulkan GPU suite 5/5. Source identity/provider-removal are in full suites; original dependency-ledger checker and `git diff --check` pass. Retail symlink read-only and unchanged.
