# M22 07B2 original status-circle physical source frame evidence

The guarded CPU-only original `RTS2DScene::draw` now checks for a live `OriginalGpuEdge` and camera, then delegates to its authored `WW3D::Render(this,m_camera)` path. The original Windows branch is unchanged. The original `W3DStatusCircle::initData` preset comes from accepted 07B2A's WW3D lifecycle, not fixture initialization. Its team-dot and fade source shaders intentionally bind null stage-0 textures and use vertex diffuse color; the Linux CPU source policy disables texturing for those two authored shaders only. Generic missing-required-texture rejection remains active, including existing decal negatives.

The owned scenario probe initializes original GameLogic, ScriptEngine, GlobalData and the asset manager before constructing the original scene. It advances the original startup fade to `FADE_NONE`, uses the source-equivalent 2D camera and identity world state, and owns a bounded WW3D frame through a public device edge. Recording sees the original status draw; no-dot and shell controls issue no draw. Injected buffer-upload and draw failures abort source frames with no active pass; retries draw. A destroyed depth target rejects, rebind recovers. The source subtract-fade `D3DBLENDOP_REVSUBTRACT` state rejects at the CPU DX8 boundary before a frame. The existing no-edge and new missing-camera negatives remain typed. WW3D and scene release before the edge; both CPU and device resources return to zero.

Validation-enabled public bgfx Vulkan readback checks BGRA8/RGBA8 at 160×120 and 200×150 in four fresh device generations. The CTest entry explicitly sets `VK_INSTANCE_LAYERS=VK_LAYER_KHRONOS_validation` and uses bgfx debug mode; a verbose run confirms the layer setting. Team-dot-on pixels differ from team-dot-off and shell backgrounds, with changed pixels in the authored upper-right region; status-on/off comparisons use fully completed public readback. Fresh-process Khronos-layer stress passes 30/30 GCC Debug and 30/30 Clang Debug host Vulkan runs. That 07B2 runner did not yet reject non-fatal validation output; 07B2C closes this separate observability gap. Sanitized GPU readback is not used as a leak oracle because driver D-Bus allocations contaminate LSan; source-only sanitizer tests remain leak-capable.

Four rebuilt GCC/Clang Debug and GCC/Clang ASan+UBSan asset-free suites pass 189/189 each. The four focused rebuilt source/identity tests pass 5/5 per preset; sanitized source-only runs retain leak detection. Source identity, ABI, provider-removal and aggregate ledgers are clean. A sandbox-only LSan ptrace fatal was reproduced across unrelated focused tests; the same leak-capable tests pass on the host, and no LSan option was disabled for these CPU gates. The production display factory, full fade rendering, other 2D/3D consumers and retail scene acceptance remain later 07/08/09 slices.
