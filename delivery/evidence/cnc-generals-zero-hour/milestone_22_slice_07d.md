# M22 07D original W3DView source camera/frame evidence

The full-draw target now compiles and links canonical `W3DView.cpp` and its original `ParabolicEase.cpp` member-constructor dependency. The Linux CPU branch constructs the original view class, owns original 3D/2D `CameraClass` refs, and routes `Display::drawViews` → `W3DView::drawView` → its guarded `draw` → original `RTS3DScene::doRender(m_3DCamera)` inside an externally bound `WW3D` frame. The initialized source fixture configures the view-owned 3D camera for its generated asset; native tactical/terrain camera transform, display draw, filters, wireframe, movement and effects remain typed pending. Original header/class layout and native branch are unchanged.

The initialized GameClient test checks no-edge and no-display-owner init rejection, camera re-entry/refcount, display view-list ownership, empty-frame no-draw, generated rigid W3D object draw, injected draw abort/retry, stale depth target rejection/rebind, detach absence, advanced update/filter/wireframe rejection, and zero owners/resources at shutdown. Public bgfx Vulkan readback compares empty versus rigid and detached pixels in BGRA8/RGBA8 at 160×120 and 200×150 across four device generations. Child `Validation Error`/`VUID-` output rejects under explicit Khronos layer. GCC and Clang Release final binaries each pass 30/30 fresh-process physical runs.

All five presets were fully rebuilt, including the presentation identity executable after the added full-draw objects. Strengthened source/linked-object/provider-removal checks cover original W3DView and ParabolicEase. GCC Debug/Release, Clang Release, GCC ASan+UBSan and Clang ASan+UBSan full host asset-free suites each pass 194/194 with clean ledger and leak-capable source-only controls. Sanitized driver-process leak reports are not claimed as source leak evidence. Production factory/display draw, tactical terrain/shroud/tracks/particles/shadows and retail scenes remain pending later 07/08 slices.
