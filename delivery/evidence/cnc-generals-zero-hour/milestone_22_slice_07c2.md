# M22 07C2 one original rigid RTS3DScene object evidence

The source `RTS3DScene` default CPU branch now admits at most one original rigid `MeshClass` without `DrawableInfo`, retaining native class layout and rejecting additional objects, other categories, lights and advanced passes. It traverses the original visibility/update/render-one-object and flush methods. The generated untextured `TEST.ZERO01` W3D packet is emitted by the existing original CPU graph fixture, then loaded by `WW3DAssetManager` in an initialized GameClient scenario. No retail file is edited or required.

The Recording probe observes a draw only while the mesh is attached. It proves initial/scene/detached refcounts of 1/2/1, unresolved asset absence, second-object rejection, camera-hidden absence, injected upload and draw abort/retry with no active pass, and zero resources after shutdown. The physical probe compares each rigid frame against its own clear-only absent frame; pixels differ while visible, then equal the absent frame when camera-hidden and after detach. It runs BGRA8/RGBA8 at 160×120 and 200×150 across four fresh bgfx Vulkan device generations, with explicit Khronos validation and child stdout/stderr rejection for `Validation Error`/`VUID-`.

GCC and Clang Release physical gates pass 30/30 fresh processes each. GCC Debug, GCC Release, Clang Release, GCC ASan+UBSan, and Clang ASan+UBSan focused source tests pass. Five full host asset-free suites pass 192/192 each (GCC Debug/Release, Clang Release, GCC/Clang sanitizers); this includes leak-capable source-only sanitizer controls, source identity/provider-removal and clean dependency ledger. Sanitized driver-process leak reports are not claimed as source leak evidence. Original terrain, shroud, shadows, effects, broader object families and production `W3DDisplay` remain guarded for later 07 children.
