# M30 slice 04 — public bgfx presentation and renewed acceptance

Status: M30 device-edge acceptance on the single required Linux RTX/Vulkan host. This is synthetic/device-grade evidence. M22 still owns actual original WW3D call-path wiring and full retail-scene pixels.

## Physical backend and host

- Pinned public bgfx `81d81fba72c42d348c589514c774bbfe01e110fa`, bx `25315498841259323e18f549d1ad9d9aba6632cc`, bimg `87aaad3ac882e741889fdd4263224e5d12c26f99`; M29's reviewed shaderc-only patch and 39 offline shader families remain unchanged. GCC 16.2.1, Clang 22.1.8, SDL3 3.4.14, Linux x86-64.
- Host `vulkaninfo --summary`: NVIDIA GeForce RTX 4070, Vulkan device API 1.4.341, NVIDIA 610.57.04, Khronos validation layer 1.4.357. Commands below explicitly enabled `VK_INSTANCE_LAYERS=VK_LAYER_KHRONOS_validation`; output had no emitted `Validation Error` or `VUID-`. No private Vulkan call was added.
- Recompiled and reran the exact checked `tools/renderer/bgfx_viewport_clear_probe.cpp` against the pinned shared runtime and pinned SPIR-V cube shaders: exit 0, 160×120 red outer/blue inset boundaries and independent depth-LESS/stencil-EQUAL green inset pixels all pass.
- `BgfxGpuDevice` receives SDL3 Wayland/X11 native window/display properties but SDL3 retains window/event/input ownership. It creates a public bgfx swap-chain framebuffer, samples the completed engine color target with the owned video shader, submits a fullscreen triangle, and advances the frame. A 160×120 SDL3 window accepts a presentation of a green-cleared source, changes actual reported pixel extent to 200×140 and updates the swap chain, releases/reclaims, then completes a second device init/present/shutdown generation. Swap-chain contents are not independently read back; green pixels are verified on the offscreen source, while the window test proves public swap-chain submission/lifecycle. Null/double claim, stale/uninitialized source, mid-pass present and post-release present reject explicitly. A narrow SDL pixel-extent query seam deterministically returns zero after a real claimed window: present suspends successfully, then an injected failed query returns an explicit error, and the same live source recovers on the next valid query. The current Wayland compositor did not itself produce zero pixels, so the suspension is seam-tested, not a physical compositor-minimize claim. No GPU-device-loss event was available; release/reclaim and query-failure propagation are tested lifecycle proxies.

## Renewed coverage and historical grade

| Earlier contract/grade | New public-bgfx evidence | Boundary |
|---|---|---|
| M2/M7 backend-neutral opacity, validation, recorder sequencing | Four-preset contract/recorder suites; stale handles/targets, LOAD, view budget, source-shaped draw→camera clear→draw physical pixels | Preserved CPU contract plus new physical ordering; old SDL_GPU result is not counted |
| M8 UI, font/input and alpha behavior | Actual `renderer/ui.vert`/`.frag` physical pixels: half-pixel coordinates, sampled green, alpha-reference discard then allow; SDL3 owns platform/input | Font shaping, cursor and retail UI scene pixels remain prior CPU/history or M22 integrated work, not newly claimed |
| M9 world/terrain semantics | Actual `renderer/world.vert`/`.frag` material pixels with frame/object matrices, normal/light, four bound texture slots and material uniform; original-FVF 16-bit indexed/base-vertex pixels | Full faction/terrain scene-family pixels are M22, not substituted by a triangle |
| M10 effects/video/point behavior | Actual post-effect texture/color-scale pixels; video texture+viewport pixels; point shader frame/object/material blocks and size; source-alpha blend | Individual projector/bump/stealth/water/WWShade family binaries physically create, but their complete M14 scene matrix is not replayed |
| M14 SDL_GPU hardware matrix | New device gives offscreen color/depth/stencil/readback, stencil 0x80 write-mask preservation, source-shaped ordered clear/draw, actual UI/world/effect/video/point/FVF pixels, SDL3 present/resize/re-entry under RTX validation | M14's 16-scene SDL_GPU and retail+GPU harnesses remain historical; not relabeled as bgfx acceptance |

`zh_renderer_bgfx` still links `zh_renderer_sdl_gpu` for shared backend-neutral contract/validation symbols and SDL3 dependencies; the new GPU tests instantiate `BgfxGpuDevice` directly, and all physical resource/draw/present calls above use public bgfx. The old `SdlGpuDevice` stays available for historical diagnostic tests only. `zh_main` is intentionally headless/recording; M22 owns `zh_original_main`/WW3D production call-path connection to this completed device seam, so no original-source retail frame or installed-runtime acceptance is claimed here.

## Validation and recoverable findings

- GCC Debug, Clang Debug, GCC Release and Clang Release native presets all build. The complete asset-free CTest suite passes **174/174 in each preset** on the host. The sandbox run initially denied ordinary UDP sockets in two LAN tests (`Operation not permitted`); the host rerun passed both and all 174 tests. No LAN code changed.
- GCC/Clang Release initially failed only `original_w3d_cpu_identity`: optimized linking omitted an unreferenced `stripoptimizer.cpp.o` despite compilation. The CPU graph test now directly executes `StripOptimizerClass::Get_Strip_Index_Count` and checks its output using that test file's locally redefined always-executed `assert` macro (the file undefines standard `assert` after includes, so `NDEBUG` cannot remove the call). Both Release link-map identity tests pass; the `original_w3d_provider_removal` negative passes and still detects missing required providers. Original source files were not modified.
- Focused ASan CPU renderer/recorder/UI/world/effects CTest: **6/6 pass with LeakSanitizer enabled** on the host. The ASan physical GPU test detected **9258 bytes in 15 allocations entirely attributed to external libdbus/Wayland/system stack** after SDL3 window teardown; no project allocation appears in that report. Physical ASan with `ASAN_OPTIONS=detect_leaks=0` passes and is graded as address-safety only, not a leak-free system GPU-stack claim. Focused UBSan CPU CTest **6/6 pass** and `UBSAN_OPTIONS=halt_on_error=1` physical RTX GPU suite passes. Sandbox ptrace prevented LeakSanitizer startup; host permission was used for its meaningful run.
- Physical `renderer_bgfx_device_tests --gpu` passes on GCC Debug, Clang Debug and GCC Release under the explicit Khronos layer, including source-shaped order, UI/world/effects pixels and window lifecycle. Exact probe passes separately. No validation errors/VUIDs were emitted in these outputs.

No retail root was written, copied or hashed; no retail bytes, private file paths or device UUIDs are recorded. The remaining original-source and full scene-family work is the already ordered M22 06C3C handoff, followed by M24. This milestone does not use historical SDL_GPU hardware checks as new-backend proof.
