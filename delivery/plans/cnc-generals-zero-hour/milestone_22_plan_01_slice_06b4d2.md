# M22 plan 01 slice 06B4D2: complete original 06B pass interleave

Requires accepted 06B4D1. Build an owned original W3D scene through canonical asset manager, `RenderInfoClass`, original HLOD/HTree skin and rigid objects, original `MaterialPassClass` immediate and cull-volume APT/dynamic-index plus rigid delayed override, and original `DecalSystemClass` rigid and skinned owners. A single original `DX8MeshRendererClass::Flush` must show rigid category→skinned category→source `ZBIAS=8`→both queued decal draws→`ZBIAS=0`→delayed rigid pass, with source state/material/texture/index bytes and source ownership before every translated public GPU draw. Recording order and separate validation-layer SDL_GPU Vulkan pixels for the *same source-scheduled aggregate frame*, resize/recreation and bounded teardown; never a direct `Peek_Decal_Mesh` or generated scene substitute. Required source texture/material, invalid bias/index/resource, disabled/translucent/no-APT, sorted skin and failed later draw/upload must reject/reset/requeue without stale owners. Reconcile every reached 06B ledger route and audit retail-family encounter evidence explicitly (full campaign/skirmish in 08). GCC/Clang full/LAN, sanitizers, identity/provider removal, six-plus validation-layer GPU tests gate the D2 commit and 06B aggregate acceptance; 06C/07/08/09 remain mandatory.
