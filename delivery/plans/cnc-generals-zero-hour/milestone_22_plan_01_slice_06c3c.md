# M22 plan 01 slice 06C3C: original scene/static-sort/frame sequence

Requires accepted 06C3B3B. Restore canonical `WW3D::Render(SceneClass*,CameraClass*,...)` and `Render(RenderObjClass&,RenderInfoClass&)` source camera Apply, fill mode, ambient/light-environment, scene/object Render and `Flush` order. Keep `TheDX8MeshRenderer.Flush()`→authored `SHD_FLUSH` branch→`Render_And_Clear_Static_Sort_Lists`→`SortingRendererClass::Flush()`→pending-delete release. Preserve original static levels descending, source publication/refcounts, disable while draining and reentrant render behavior; do not compile an adapter-owned scheduler or accept direct `TheDX8MeshRenderer.Flush` as a scene witness. An owned original scene/object frame with at least two static levels, queued translucent meshes, accepted rigid/skin/decal categories and original camera proves exact command/order/refcounts on Recording and validation-enabled Vulkan with visible pixels; failure during each category/static/sort route must unwind/reset/requeue with no stale owners, and repeat at two dimensions/device generations. Source camera-viewport clear must preserve untouched pixels and depth; explicit unsupported fill/clear/scene variants reject before partial publication. GCC/Clang full/LAN, focused sanitizers, source identity/provider/ABI and ledger gate separate C commit and 06C3 aggregate acceptance. 06C4 still owns WWShade branch/family and complete 06C aggregation.
