# M22 plan 01 slice 06B3: original additional and delayed material passes

Requires accepted 06B2; deliver aggregate through B3A no-cull source immediate skin/rigid pass, B3B original delayed timing, B3C cull-volume APT/dynamic index branch. Restore original `MeshClass::Render_Material_Pass` skin and rigid behavior, `MaterialPassClass::Install_Materials`/`UnInstall_Materials`, source visible/procedural and delayed scheduling, original per-instance opacity/emissive restoration, source world identity/transform, optional cull-volume APT and `DynamicIBAccessClass` indices. No adapter-defined pass, shortcut around source temporal interleaving, or swallowed required draw failure. Preserve authored nonempty delayed-task timing and retain the existing sorted route ownership for 06C.

Owned W3D/material-pass fixtures distinguish original skin/rigid, cull/no-cull, additional immediate/delayed and empty queue, assert original pass ordering and exact buffer/index ranges plus 0/1/2 texture stages as reached, with positive Recording and validation-enabled Vulkan pixels. Missing required material/texture, invalid polygon/indices, unsupported reached state and injected bind/draw failure unwind/retry without stale queue/material override. Source-retail reachability inventory separates required positive routes from typed-unavailable non-encountered branches with evidence; nothing silently succeeds. GCC/Clang full/LAN, identity/provider-removal, sanitizer and GPU suite pass before each separately committed B3A/B/C slice. No B3 aggregate claim until C.
