# M22 plan 01 slice 06C1A: canonical source sort state and refs

Requires accepted 06B. Share the exact original `RenderStateStruct` field order, constructor/destructor and assignment/refcounts from `dx8wrapper.h` across mutually exclusive native/CPU targets without a schema fork or two incompatible definitions in one binary. The Linux original `DX8Wrapper::CpuState` remains the single selected-state owner; implement canonical `Get_Render_State`, `Set_Render_State`, `Release_Render_State` so a captured copy faithfully carries original shader, material, eight texture stage pointers, world/view, four lights, up to two vertex buffers, index buffer/types/counts/offsets and engine refs. The authored struct has no GPU generation field: valid source-owned buffers must reupload into a new edge generation; stale physical handles, absent active edge, unsupported state and missing owner reject at their corresponding boundary before publishing a partial draw. Replay and error preserve source identity, exact buffer source bytes and no cache corruption. An original W3D mesh/material/buffer fixture verifies reference lifetime, snapshot/restore and source physical draw, failure/reset/retry; compiling original sorter may remain typed until 06C1B. Native ABI layout/inline semantics, link-map/provider removal, GCC/Clang full/LAN, focused sanitizers and checked ledger gate one A commit. Not sorting acceptance.
