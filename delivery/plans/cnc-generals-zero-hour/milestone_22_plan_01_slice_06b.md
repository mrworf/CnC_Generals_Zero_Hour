# M22 plan 01 slice 06B: original skin/decal/additional passes

## Dependency and behavior

Requires accepted 06A3C (the full 06A aggregate). Delivered in order by 06B1 original dynamic buffer owners, 06B2 original skin category and exact dynamic FVF, 06B3 material/additional/delayed/cull-volume routes and 06B4 decal/depth-bias routes. Extend original `DX8SkinFVFCategoryContainer::Render`, `DX8MeshRendererClass::Render_Decal_Meshes`, `DX8RigidFVFCategoryContainer::Render_Delayed_Procedural_Material_Passes`, original mesh material-pass and dynamic-buffer owners. Original skin bone/vertex transform, per-pass texture/material/shader, decal depth bias and procedural pass scheduling issue their own draw commands via the same bounded public GPU edge. Preserve preexisting rigid behavior and native source device path; no adapter-owned skinning/decals/procedural scheduling. The canonical CPU branch of original `dx8vertexbuffer.cpp`/`dx8indexbuffer.cpp` currently excludes original dynamic access methods and rejects `USAGE_DYNAMIC`; B1 must restore that source-owned closure before B2/B3/B4. The skinned source FVF `DX8_FVF_XYZNDUV2=0x252` has a diffuse vertex color and needs exact source-input GLSL in B2, not the existing normal-only variant.

## Acceptance

Owned W3D fixture proves skin, decal, additional and delayed procedural pass positive source call order/draws, exact buffer offsets and stage/material state. Negative invalid bones/indices, missing required resource, unsupported reached device state and injected bind/draw failure unwind/retry; no silently skipped pass or partial frame success. Read-only retail encounter inventory distinguishes required positive routes from source-owned but non-encountered routes; the latter retain explicit typed negative until encountered, with the ledger/evidence stating why. Original source identities, provider-removal, full GCC/Clang regression and focused sanitizers/ledger pass. Sorting/static/WWShade/full frame remain 06C. Commit each tested B1/B2/B3/B4 independently, then aggregate 06B acceptance; no accepted 06B from just one subroute.
