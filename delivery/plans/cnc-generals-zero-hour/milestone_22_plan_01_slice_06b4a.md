# M22 plan 01 slice 06B4A: canonical original decal CPU producer closure

Requires accepted 06B3. Link the smallest reached original `ProjectorClass`/`DecalSystemClass`/`DecalGeneratorClass`/`RigidDecalMeshClass`/`SkinDecalMeshClass` graph from `projector.cpp`, `decalsys.cpp`, `decalmsh.cpp`, and original `MeshClass::Create_Decal` into the single production `ZH_WW3D_CPU_ONLY` WW3D configuration. Preserve original system lock/unlock, generator volume/transform, texture/material/shader ownership, original rigid APT plus clipped UV/color/index and HLOD/HTree skin deformation/APT/UV; no adapter-owned decal or generated substitute geometry. Device-only methods and original renderer's physical decal queue remain distinctly typed unavailable, not guarded no-ops. Preserve no-APT, disabled, translucent and sorted source decisions. Validate original owner accounting, malformed index/texture/resource failure and reset/retry in a direct original-object fixture; source identity/provider-removal and GCC/Clang full/LAN + focused sanitizers. No physical decal claim here; B/C/D mandatory.
