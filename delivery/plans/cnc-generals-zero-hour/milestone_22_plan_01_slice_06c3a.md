# M22 plan 01 slice 06C3A: original camera physical viewport edge

Requires accepted 06C2. Restore the reached original `CameraClass::Apply`: original `Update_Frustum`, normalized viewport to target pixel rectangle and 0..1 depth range, `Get_D3D_Projection_Matrix` with original projection/Z-bias selection, and `CameraInvTransform` view state execute before the physical edge. Extend public `GpuDevice`/Recording/SDL_GPU narrowly with an active-pass viewport including depth, using `SDL_SetGPUViewport`, and expose exact target dimensions only via the active device/pass contract; preserve full-viewport existing defaults. No direct Vulkan calls or adapter-authored camera matrices. Positive original camera partial/full viewport and nonidentity view/projection on Recording and validation-layer Vulkan pixels; negatives reject absent pass, zero/out-of-target/nonfinite viewport or depth, unsupported bias/camera state, and failed/set-after-reset paths without state mutation. Source ABI/provider removal, GCC/Clang full/LAN, focused sanitizers and checked ledger gate A commit. WW3D frame lifecycle remains 06C3B; scene scheduling 06C3C.
