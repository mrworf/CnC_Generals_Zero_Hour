# M22 08E1C0B2B1: generated start-marker label helper

Invoke source `updateMapStartSpots` over generated C0A1 GameInfo/GameSlot,
C0B1 MapCache and project-owned live button windows. Prove source slot to
marker label/tooltip mapping, invalid map and slot rejection, two generations
and zero stale window/text ownership. Spatial `positionStartSpots` is deferred:
it additionally composes preview, additional-image and region/window geometry
providers, so it must consume this live gadget contract rather than mock it.
