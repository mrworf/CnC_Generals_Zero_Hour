# M22 plan 01 slice 06B3A: original immediate additional pass

Requires accepted 06B2B. Original `MaterialPassClass::Install_Materials` chooses source material/shader and bounded source textures using the public texture-stage capacity policy: Linux's two physical stages must reject nonnull source stages 2–7 before mutating pending source state; authored null material remains valid and preserves original default fallback. Original `UnInstall_Materials` no-op remains original. Restore non-cull original `MeshClass::Render_Material_Pass` skin and rigid branch, source light/world identity or transform, polygon renderer list, and per-instance opacity/emissive override restoration even when the translated device rejects a draw. Keep original nonempty immediate task queue and exact original base→additional ordering. The original cull-volume branch must select a typed unsupported physical edge here until B3C; delayed scheduling retains typed stop until B3B. No adapter pass, fixture proxy, or invented shader.

Owned W3D original rigid and HLOD skin with source `MaterialPassClass` demonstrate immediate source queue and one physical additional draw after base, exact material/shader/0–2 texture stages as reached, Recording order/uniform/index and Vulkan pixels. Negative malformed/required source texture or unsupported nonnull stage 2–7, selected cull mode and injected upload/draw failure abort/requeue without stale queue or modified shared opacity/emissive; authored optional null material/default fallback and null material with an override (original guarded no-op) remain positive. Empty queue no-op and source-level light/world differences. Retail inventory distinguishes reached pass families from typed non-encountered. GCC/Clang full/LAN, sanitizer, original link/ABI/provider-removal and validation-layer GPU tests pass before one B3A commit; B/C remain pending.
