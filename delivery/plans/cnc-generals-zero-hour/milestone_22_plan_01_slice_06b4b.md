# M22 plan 01 slice 06B4B: source depth-bias translation

Requires accepted 06B4A. Characterize original `D3DRS_ZBIAS` source values and public SDL_GPU rasterizer depth-bias semantics from primary documentation and installed SDL headers; implement only a bounded exact mapping for source `0` and required authored decal `8` (plus any retail-required value discovered). Existing public `PipelineDesc::raster.depth_bias` and SDL_GPU rasterizer field are the device boundary: extend only if that public contract cannot express required semantics. Original `DX8Wrapper::Set_DX8_Render_State` must retain state/negative decisions; the original state cache must include selected bias in the pipeline descriptor without silently ignoring it. Recording descriptor and SDL Vulkan overlapping-depth pixel tests under explicit validation prove bias begin/reset, pipeline reuse/key distinction and unsupported-value negative. No decal mesh/category acceptance before C/D. GCC/Clang full/LAN, sanitizers, identity/provider and GPU5 gate one B commit.
