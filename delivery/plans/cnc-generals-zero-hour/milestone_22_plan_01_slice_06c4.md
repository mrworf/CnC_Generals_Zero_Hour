# M22 plan 01 slice 06C4: authored WWShade branches and complete source frame

Requires accepted 06C3. Preserve both compile-time `USE_WWSHADE` branches exactly: current production configuration omits the define, retaining original MeshLoader and no-op `SHD_FLUSH`; a guarded enabled configuration switches loader to `SHD_REG_LOADER`/SHDMESH and executes actual `SHD_FLUSH` and reached shader-family behavior if required by original retail assets. Establish read-only retail W3D chunk and shader-family aggregate without storing private names/paths/hashes; document reachability and exercise each enabled required family or reject unsupported required families with an explicit contract escalation, never silently skip. Keep disabled branch negative witness and original regular material/effect scheduler semantics. Complete owned rigid/skin/decal/immediate/delayed/cull/static/translucent/sort WW3D frame and branch-relevant WWShade-selected source order on Recording and validation-layer public bgfx Vulkan, with visual pixels, missing required assets/shaders, wrong ordering, stage/bind/draw/flush failure abort/requeue, device recreation and bounded teardown. GCC/Clang full/LAN, focused sanitizers, source identity/ABI/provider and checked ledger gate one C4 commit and 06C aggregate acceptance. Full retail GameClient scene 07/08 and final M22 09 remain mandatory.
