# M24 slice 06: cumulative original persistence acceptance

## Goal and observable outcome

M24 save/load/replay and deterministic state meet the whole source contract on project-owned and separately gated read-only retail scenarios, with four-preset and sanitizer evidence ready for PRE-033.

## Scope / non-scope

Own cumulative identity/ledger/failure checks, retail source replay, exact first simulation tick after retail load, four-preset full suites and final evidence. Narrowly repair newly evidenced M24 runtime defects. Do not claim M22 renderer, M23 interactive media, M25 LAN or Windows compatibility.

## Dependencies, entry and state

Slices 01–05B complete, including M22 closure required by 05B. Retail mission/skirmish setup already works under M21. The first retail mission `GameEngine::update` currently segfaults in the headless runtime; locate its exact source consumer and either repair within M24 scope or report a proven dependency/authority blocker without bypassing the tick. Original `.rep` playback remains through `RecorderClass::playbackFile`, `MSG_NEW_GAME`, `InitRandom` and source command dispatch.

## Permissions, validation and errors

Retail roots/symlink are read-only; keep every output under isolated XDG and commit only derived logical results, never retail bytes, names or hashes. Corrupt real save/replay, absent maps and wrong CRC must fail with unchanged live state at precommit boundaries. Sandbox socket denial for LAN tests must be separately characterized, not counted as an M24 regression or silently omitted from full-suite reporting.

## Expected surfaces

`tests/original_persistence/*`, CMake gated registrations, source identity/provider-removal checks, `docs/original-runtime-dependency-ledger.tsv`, `evidence/qa/cnc-generals-zero-hour/M24-original-persistence.md`, governing plan and milestone handoff. Production edits only for evidenced M24 later-path defects.

## Tests and commands

- Positive retail mission/skirmish save/load, original command record/playback and first post-load simulation tick; exact frame, object/player/team/script/AI/partition and full CRC checkpoints across GCC/Clang Debug/Release.
- Corrupt original save/replay, failure injection, path traversal, absent source providers and headless diagnostic controls; project-owned default runs independent of retail/device.
- `cmake --build --preset <each>` and `ctest --preset <each> --output-on-failure` for all four presets after builds complete; Clang ASan/UBSan; source ledger/drift checks. Report environment-denied socket tests explicitly and rerun them only when authorized network capability is available.
- Read-only retail metadata check before/after with no private material in committed evidence.

## Acceptance / commit

Every M24 clause has source-owned positive/negative evidence and no unresolved M24 runtime defect. If first tick or replay requires an unsettled M22/M23/M25 authority decision, record the exact dependency and stop rather than weaken this gate. Commit as `delivery: M24 slice 06 accept original persistence`.
