# M22 plan 01 slice 06C3B3A: source texture-frame update and memory statistics

Requires accepted 06C3B2. Source inventory found `WW3D::Begin_Render` calls the canonical `TextureLoader::Update(network_callback)` before dynamic VB/IB resets and `Debug_Statistics::Begin_Statistics`; the CPU configuration currently lacks `TextureLoader::Update`, while its texture invalidation method is a no-op in `texture_cpu.inc`. Original `WWMemoryLogClass` frame-count methods are likewise absent from the linked CPU target. Restore `TextureLoader::Update` within the canonical `textureloader.cpp` CPU branch, preserving the original suspended/no-pending-queue callback rule (synchronous CPU load tasks may not fabricate background queues or callback ticks). Move the *single* canonical original `TextureBaseClass::Invalidate_Old_Unused_Textures` body into a shared region of original `texture.cpp` for both builds, delete the CPU no-op and preserve source thumbnail, apply count, sync-age/inactivation override and procedural handling. Compile/link the reached original `wwmemlog.cpp` in its authored Linux disabled-counter profile, adapting only Win32 include/compile dependencies and keeping allocator policy unchanged; source-owned `Debug_Statistics` already exists. Direct original CPU fixture proves expiry/retention at sync-time boundaries with owner/generation reupload, suspended update, no callback with empty foreground queue, original disabled-counter profile, negative missing asset-manager/failure state and teardown. No WW3D frame/begin/pass claim until B3B. GCC/Clang full/LAN, focused sanitizers, original identity/provider-removal/ABI and checked ledger gate independent A commit.
