# M22 plan 01 slice 06B2A: exact source dynamic FVF and material-color selector

Requires accepted 06B1. Extend bounded physical GLSL pipeline input only for canonical original `DX8_FVF_XYZNDUV2=0x252`: position, normal, source BGRA diffuse COLOR1, UV0 and UV1 at exact `FVFInfoClass` offsets/stride. Preserve original `VertexMaterialClass` material-source selectors (MATERIAL/COLOR1/COLOR2) and `D3DRS_COLORVERTEX` source decisions in lit per-vertex ambient/diffuse/emissive/specular; when COLOR2 absent, retain documented fixed-function material fallback. The original source shader/material/wrapper chooses state before the device-edge translation. No generated skin shape, adapter-selected material, or category claim.

Direct original vertex buffer + original source `VertexMaterialClass` fixture demonstrates MATERIAL default and authored COLOR1 RGB/alpha under 0/1/2 original texture stages as reached, with exact Vulkan pixels and Recording uniforms/variant. Invalid missing normal, unsupported element/selector, misaligned FVF and source-generation/stale owner reject without leaked resource or previously prepared-state corruption. GLSL variation is strictly keyed to FVF 0x252 and audited against source layout; all existing B1/06A variants unchanged. GCC/Clang full/LAN, provider-removal/ABI/ledger, sanitizers and explicit Khronos validation zero VUID before a single B2A commit. B2B still owns original skin category draw.
