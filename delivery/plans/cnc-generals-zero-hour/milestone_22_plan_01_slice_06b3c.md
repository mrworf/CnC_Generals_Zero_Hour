# M22 plan 01 slice 06B3C: original cull-volume APT and dynamic index

Requires accepted 06B3B. Restore the original `MeshClass::Render_Material_Pass` cull branch: original OBB-to-local transform, view direction, original mesh APT/cull tree selection and `DynamicIBAccessClass` 16-bit index lock, min/max/vertex offset, source `Install_Materials`, world transform, physical indexed draw and no-op when no polygons qualify. Resolve the original `IndexBufferExceptionFunc` ownership and preserve source behavior rather than swallowing malformed polygon errors; fail closed on 16-bit conversion/index overrun. Sorting dynamic route remains 06C's typed boundary unless retail/family evidence makes it required earlier. Do not replace APT or infer geometry from an adapter.

Owned cull/no-cull W3D and pass fixtures compare exact APT source selected polygons, dynamic index bytes/offset and draw count with original rigid/skin immediate and delayed frame order; real SDL_GPU Vulkan pixels demonstrate qualified geometry and absence outside volume. Malformed polygon/index, unsupported sorting/device state and injected upload/draw failure abort/requeue/reset without stale material or resource; bounded teardown. Read-only retail family inventory defines encountered variants without weakening later retail acceptance. GCC/Clang full/LAN, sanitizers, source identity/provider-removal/ledger and GPU validation pass before B3C commit and 06B3 aggregate acceptance.
