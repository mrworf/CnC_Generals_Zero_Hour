# M22 plan 01 slice 06B4D1: original Flush decal queue and rollback

Requires accepted 06B4C. Restore original `DX8MeshRendererClass::Render_Decal_Meshes` source-owned visible list after rigid/skin categories and before delayed rigid passes; invoke original concrete rigid and skin `Render()` with source `D3DRS_ZBIAS=8` before traversal and `0` on success and every error path. No synthetic device queue or direct accessor acceptance. Original `MeshClass::Render` selects decal visibility and queue; retain disabled/translucent/no-APT/sorted branches. Inject first and later decal upload/draw failures after accepted base, ensure frame abort is explicit, bias resets and no stale queue pointers survive owner deletion; requeue through original Mesh::Render and retry in a new original physical session, with bounded resources. Source identity/provider/ABI, GCC/Clang full/LAN, sanitizers and existing validation-layer GPU suite gate separate D1 commit. D1 does not accept 06B aggregate: D2 still owns same-Flush all-pass scene and Vulkan pixels.
