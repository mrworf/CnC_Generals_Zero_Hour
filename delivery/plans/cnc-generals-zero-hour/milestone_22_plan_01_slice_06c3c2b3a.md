# M22 plan 01 slice 06C3C2B3A: first source scene/static bgfx pixels

## Outcome and boundary

Requires B2. Original `WW3D::Begin_Render`→`WW3D::Render(scene,camera)`→`WW3D::End_Render` must submit a source asset-manager rigid mesh and two original static-sort levels through the accepted public bgfx device. Bounded RGBA readback at two extents/device generations must distinguish known source clear-only pixels from mesh-covered pixels and viewport boundaries. This is the first physical source scene/static gate, not acceptance of mixed-category B3 or retail.

## Invariants and negative controls

Keep original `SimpleSceneClass` membership, `MeshClass::Render`, source sort-level queue, descending drain, and C1 viewport clear; no adapter draw may substitute. Record source markers and list/refcount order in Recording first, then run RTX validation-enabled bgfx. Pixel oracle: an outer pixel outside source viewport remains the known full-frame clear; a source rigid triangle changes covered pixels inside the viewport; adding the two original static levels changes at least one additional covered pixel relative to a same-clear rigid-only negative-control frame. Hook order at disabled-list drain is level 2 then level 1, and removing either static object must remove its distinct contribution. Reject stale target or inactive frame before claiming pixels, and retry a new generation without stale resources. Do not read/modify retail content.

Reached source scheduling prerequisite: `DX8MeshRendererClass::Flush` unbinds selected source VB/IB after each native category flush, but its CPU branch had guarded out that cleanup. A newly registered static mesh can then hit `AppendLockClass` while the old buffer still holds engine refs. Restore the canonical end-of-Flush source unbind for CPU before accepting any static-scene trace; test held-ref failure injection and retry, and preserve no stale selected buffer across device generations. Do not bypass the append lock or pre-register all static meshes solely in the fixture.
The CPU wrapper's null-buffer setters must perform that source cleanup even after an expected missing-session render failure; only non-null bind and draw require a live GPU edge. Reconcile older fixture assertions that expected the now-incorrect held engine refs after Flush, and rerun their tests.

Reached physical attachment prerequisite: original applied pipeline descriptors default to RGBA8, but the accepted public bgfx scene target is BGRA8. The first RTX source rigid draw rejects a pipeline/active-target mismatch. Derive the pipeline color/depth format only from the already bound caller-owned public target's live generation-checked format query at preparation time, preserving original material/raster decisions and explicit rejection of stale/incompatible targets. Prove both BGRA8 and RGBA8 source scenes, plus stale target query; do not change the fixture to RGBA8 to evade production BGRA8 compatibility.

Reached one-mip sampler prerequisite: the first RTX source indexed draw uses a live, initialized one-mip TextureClass and live sampler with source `TextureFilterClass` mip=NONE/maximum LOD 0. Public bgfx currently rejects every maximum LOD except 1000, although sampling the sole available mip at LOD 0 is physically equivalent. Admit LOD 0 only for a one-mip sampled texture; retain fail-closed validation for stale/uninitialized resources and all other unsupported LODs or multi-mip requests. Record the first failing source draw, a direct physical positive and negative, and source-scene pixels before accepting this closure.

Second-generation Vulkan-validation stress has exposed an intermittent native render-thread fault after a completed first source scene even though a device-only two-generation RGBA8 control passes. Keep BGRA8/RGBA8 and two-generation source controls mandatory. Before acceptance, instrument public handle counts at original edge/session and target teardown, verify source buffers/textures are retired before device destruction, and isolate whether cross-generation source ownership or deferred native release is responsible; no timing-only suppression or validation bypass.

The first corrected fixture still leaves stack-owned `SimpleSceneClass` and `CameraClass` alive until after canonical `WW3D::Shutdown` and asset-manager free, and ASan without readback intermittently jumps through an invalid scene virtual call on the second frame. Scope those source objects (and hook-bearing render objects) to destruct after explicit scene removal/ref release but before `WW3D::Shutdown`; keep per-generation asset loading and teardown assertions. Re-run bounded ASan no-readback and readback stress, and retain independent evidence for any native shutdown stall. Do not change production ownership on fixture speculation.

Before classifying the remaining failure, add diagnostic-only fixture controls for repeated Recording generations and exactly one bgfx source generation per fresh process. Compare bounded GCC/Clang ASan+UBSan one-generation no-readback runs with Recording repeats, and verify compile commands instrument the original `zh_w3d` translation units and `original_gpu_edge`. Capture the earliest failing operation/stack; if only physical execution fails, narrow which public device operation introduces corruption. Keep the normal two-format/two-extent gate unchanged and do not accept from diagnostic controls.

If the re-entry-only control fails in Vulkan validation, add a bounded one-source-frame-per-generation diagnostic (rigid source mesh only) and compare it with a source clear-only frame. This separates the first public `begin_pass`/clear/end submission from the first original draw and category Flush. Keep the normal source/static scene and pixel assertions untouched outside these explicit diagnostic controls.

If a clear-only second-generation run corrupts asset material state before its frame, add an asset-only generation control: still create a public bgfx device and caller targets, original edge, `WW3D::Init`, W3D asset-manager load/create/release, and canonical `WW3D::Shutdown`, but omit source frame submission. This isolates public device lifecycle/target creation from source GPU commands. Never infer a rendering pass from that diagnostic; use it only to locate the first failure.

If asset-only stays clean, reproduce the same one-pass outer clear plus inset viewport clear on two public bgfx device generations with no original runtime or assets. Compare that controlled device-only path to the source clear-only path before changing any public bgfx deferred-destruction behavior. Preserve existing M30 device contract tests and keep this diagnostic explicitly separate from acceptance.

If direct public clears stay clean while source clear-only fails, compare original Begin→End without SceneClass::Render (outer clear only) to Begin→Render(empty SceneClass)→End (outer plus camera inset clear and source Flush). This narrows whether the first unsafe transition is source frame lifecycle or camera/scene processing. Use the same asset/device generations, with no geometry draw, and do not use the reduced diagnostic as pixel acceptance.

Compare exact public outer begin_pass→set_viewport→end_pass with no inset and source Begin→End under Recording across repeated generations. This bounds the gap between the two otherwise similar paths; log all selected diagnostic modes in evidence and retain normal B3A acceptance cases untouched.

Next, with the same original asset/edge/device lifetime, call only `OriginalGpuEdge::begin_source_frame`→`end_source_frame` (no `WW3D::Begin_Render`/`End_Render`) as a diagnostic. Compare it to original Begin→End and inspect source static/global caches and edge/device resource counts before/after each transition. If the direct edge frame is clean, selectively suppress one original Begin/End CPU side effect at a time in test-only controls; do not patch production lifecycle speculatively.

If the edge-only run stalls after all frame/done markers, add temporary opt-in stage markers around public bgfx teardown (resource destruction, frame drain, shutdown) and capture a bounded render-thread stack at the first stalled stage. Keep these markers gated and remove or justify them before slice acceptance; do not treat an unpolled process as passed.

Source Begin calls `TextureLoader::Update` before edge begin. The CPU branch invalidates old unused textures; compare edge-only pass with an explicit original TextureLoader update, and update-only with no pass, under the same asset/device generations. Record live physical resource counts and whether source mesh material pointers remain valid at each transition. Do not assume the update is harmless or skip it in production without source-semantic proof.

The fixture disables thumbnails, so original texture invalidation returns early; an edge+update failure is not evidence of texture invalidation. Use a same-executable, same-target/wait/destructor `device_pass` control with no original edge/WW3D, then perform bounded ≥50 fresh-process two-generation comparisons against edge-only. This controls executable/layout and preserves the normal source gate. Stop before attributing blame from small stochastic differences.

If asset-only also corrupts before its first pass, diagnose the generated W3D packet and original class-pool allocator. A separate diagnostic build may compile `W3DMPO_GLUE` as plain global `new/delete` for the CPU-only original W3D code (same definition across all affected translation units) to expose use-after-free to ASan; it is not a product allocation-policy change. Add pool/packet observations and an exact first-failure stack before choosing a production fix.

The original per-class allocator ignores the requested `operator new(size_t)` size in non-debug builds after a debug-only equality assertion. A derived class inheriting a base pool `operator new` could overflow its fixed block. Add opt-in diagnostic runtime pool-size checks in `allocateFromW3DMemPool` (both overloads) and run the original asset-only fixture before any production policy change. If a mismatch appears, capture concrete class/size and fix that source ownership/allocator boundary narrowly; if none appears, continue pool UAF investigation.

## Tests and commit

Add a focused CPU Recording test and public-bgfx Vulkan test with explicit expected outer/viewport/triangle pixels, two extents and new device generation, validation-output scan, and resource-count/refcount cleanup. Run GCC/Clang source and renderer focused tests, ABI/provider removal, dependency ledger and relevant sanitizers. Evidence `delivery/evidence/cnc-generals-zero-hour/milestone_22_slice_06c3c2b3a.md`. One commit: `delivery: M22 slice 06C3C2B3A render source static scene on bgfx`.
