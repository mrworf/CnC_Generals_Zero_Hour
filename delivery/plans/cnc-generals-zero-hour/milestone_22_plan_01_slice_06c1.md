# M22 plan 01 slice 06C1: original source sort queue and CPU pool decisions

Deliver through 06C1A shared canonical source state/refcounts then 06C1B original sorter CPU queue/pool. No 06C1 acceptance until both are tested and committed; the original behavior and subsequent 06C2–C4 gates below remain unchanged.

Requires accepted 06B. The canonical original `sortingrenderer.cpp` currently compiles only a CPU typed `Flush` throw; the disabled native body owns `SortingNodeStruct` with `RenderStateStruct`/refs, original depth transform and sorted-list insertion, overlapping sorting pool, per-triangle depth/source index reorder, and cleanup. The current CPU `DX8Wrapper::CpuState` is source-owned selected GPU state, but native inline `Get_Render_State`/`Set_Render_State` refers instead to native-only `render_state` and `CurrentCaps`; first restore an ABI-matched canonical snapshot/refcount bridge in original DX8Wrapper from that same selected state (never add an adapter second cache). Restore CPU decisions in the original sorter TU and original sorting VB/IB memory; no duplicate sorter proxy. Source `Insert_Triangles` preserves sorting-disabled direct draw and sorted/pool selector; unsupported physical allocation/draw remains a typed edge after CPU decision witnesses. Negative invalid indices, nonfinite depth, overflow, unsupported buffer type and abort/reset/retry without leaked original refs; bounded source teardown. Explicit source/ABI/link/provider controls, GCC/Clang full/LAN, sanitizers and ledger gate one C1 commit. C2 remains mandatory for physical sort, C3 WW3D schedule, C4 complete fixture/WWShade; no full 06C claim.
