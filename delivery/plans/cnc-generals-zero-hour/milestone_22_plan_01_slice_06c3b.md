# M22 plan 01 slice 06C3B: original WW3D frame lifecycle and clear

Requires accepted 06C3A. Restore original `WW3D::Init`/`Shutdown` publication and `Begin_Render`/`End_Render` IsInitted/IsRendering, texture-loader update, dynamic buffer reset, statistics, frame count, sorting drain and cached-state invalidation in the canonical `ww3d.cpp`, with only Windows DX8/timer/swap hardware calls replaced at the physical public device boundary. Explicitly preserve authored lite/full init behavior and static-list ownership; reject unsupported external device/window/recording configuration rather than faking successful initialization. Define a public, Recording/SDL_GPU parity clear/presentation mapping for source color, depth, alpha and full target/scene viewport; retain pass ownership and fail closed when a midpass or partial clear cannot be represented, with no silent substitute clear. Positive original Begin/End lifecycle and source clear/callback order on Recording plus validation-layer Vulkan pixels and two device generations; negative double/reentrant begin, end without begin, missing/failed device/pass, unsupported clear and partial-init unwind/retry. GCC/Clang full/LAN, focused sanitizers, original provider/ABI and checked ledger gate B commit. Scene/object traversal and static-sort/Flush remain 06C3C.
