# M22 08E1C0B: generated multiplayer persona/map-helper closure

After 08E1C0A2, C0B is dependency-split. **C0B1** proves generated source
ChallengeGenerals and MapCache metadata publication/lookup. **C0B2** follows
with the actual map-preview copy and start-marker helper route, which needs
GameState/GlobalData/filesystem/mapped-image resource ownership and live source
GUI gadget windows. No owner composition, retail map, network service or pixels
is included.

## Aggregate transaction

This final C0B aggregate adds no production source. Its generated-data
executable witness runs the accepted C0B1 source persona/MapCache metadata
transaction before the C0B2 preview, label, and placement transactions, and
requires every component to report its two-generation zero-ownership result.
Missing executables or altered result markers fail deterministically. The
component slices retain their direct malformed-input, failure/retry,
archive-removal, and teardown controls; this aggregate does not duplicate or
relax them.

It proves provider ordering across generated persona/map metadata and the
source map helper route, not retail content, WND layout, pixels, audio, media,
multiplayer service, or final owner composition. Required acceptance is focused
GCC/Clang coupling and ledger checks, fresh six-config non-GPU/non-LAN suites,
strict host LSan for both compilers, proportional physical Vulkan, and serial
LAN in every configuration.

Commit boundary: one aggregate test-only witness with CMake registration,
ledger, plan, and evidence in `delivery: M22 08E1C0B persona map aggregate`.
