# M22 plan 01 slice 06C3B2: explicit public attachment clear and load semantics

Requires accepted 06C3B1. Extend only the public `GpuDevice` render-pass descriptor/validation as required for original WW3D's source-selected full-target color including destination alpha, depth clear, color/depth preservation and bounded presentation. Existing pass defaults remain unchanged for M14/M20/M21 and accepted M22 witnesses. Recording and SDL_GPU lower the same explicit load/clear/store/depth semantics, reset viewport on full-target clear and reject nonfinite/out-of-range color/depth, stale/mismatched targets, unsupported midpass or partial clear and illegal begin/end/present before mutation. `SDL_GPU` clear/load operations and cycle flags must preserve previously rendered contents when load is requested, never reinterpret a no-clear as a clear. Direct Recording transitions and Vulkan validation-layer pixel/depth tests prove clear combinations and preservation across two dimensions and device generations, plus failure/retry without leaked command/pass owners. This slice does not claim original WW3D Begin/End or scene viewport clear: B3 owns source calls, C3C owns camera-scoped clear. GCC/Clang full/LAN, focused sanitizers, public contract/provider/ledger gates and independently committed B2.
