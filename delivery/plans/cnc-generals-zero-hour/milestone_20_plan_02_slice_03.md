# M20 plan 02 slice 03: complete factories and registered providers

## Goal and observable outcome

The complete original module, object, function-lexicon, particle, data-store, audio, GameClient/W3D, AI, GameLogic, player/team/crate, recorder, radar, and victory provider closure compiles and registers through the Linux engine factory. Representative real configured modules instantiate and release; unknown required modules fail visibly.

## Scope and non-scope

Promote every constructor, vtable, callback, singleton, and source body reachable from M20 startup, regardless of its historical M21-M25 classification. Bind accepted CPU presentation/media and native backend interfaces only at external device edges. Preserve complete module/object registries, including GameLogic, GameClient, and W3D entries. Later full scenarios, hardware rendering, interactive UI/media, save/replay scenarios, and LAN matches remain M21-M25 acceptance, but none may supply M20 startup code later.

## Dependencies and ordering

Requires slices 01-02 and accepted M28. Supplies complete concrete providers for slice 04 integrated update/reset.

## Entry point, state, and permissions

Original engine initialization resolves real registry entries from owned configuration, constructs each subsystem in source order, publishes its singleton, post-loads it, and releases representative instances. No external authorization applies. Offline network remains null; selecting an unsupported network path returns a clear nonzero error and never initiates traffic.

## Validation and recovery

Positive tests instantiate representative update/draw/body modules and verify their original types/state. Negative tests remove providers, request unknown modules, inject every constructor/init/registration/post-load stage, and verify reverse exactly-once cleanup with stopped workers and cleared globals. Empty registries, weak stubs, discarded-section identity, and proxy counters fail assurance.

## Expected surfaces

Original factory/registry/provider sources, Linux factory/device adapters, CMake source manifests, owned registry fixtures, source classification/dependency ledger, identity/link-map/runtime controls.

## Required validation and acceptance

Focused complete-registry tests pass in all four presets; representative modules execute source-owned behavior; unknown/removed providers fail at real gates; every required startup edge has an owner and runtime or explicit compile/link evidence. Commit as `delivery: M20 slice 03 integrate original provider registries`.

