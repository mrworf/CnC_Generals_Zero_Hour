# M22 plan 01 slice 06C3: original WW3D camera and static-sort frame scheduler

Requires accepted 06C2. Restore reached original `WW3D::Render` object/scene camera viewport/projection, ambient/lighting and clear/fill decisions at a typed public device edge, preserving original IsInitted/IsRendering/Init lifecycle and source `StaticSortListClass` publication/disable/reentrant render. Original `WW3D::Flush` order must remain `TheDX8MeshRenderer.Flush()`→authored `SHD_FLUSH` branch→`Render_And_Clear_Static_Sort_Lists`→`SortingRendererClass::Flush()`→pending-delete release, not a synthetic precomputed pass graph. Owned original scene/object with at least two static levels plus queued translucent sorting and already accepted rigid/skin/decal categories proves exact source decision/order on Recording and validation-layer SDL_GPU Vulkan; unsupported clear/fill/camera paths reject explicitly, failure abort/reset/retry without stale list or stage owners. GCC/Clang full/LAN, sanitizers, source identity/provider, ABI and ledger gate separate C3 commit. C4 still owns WWShade branch/family audit and aggregate acceptance; 07 production display/scene integration remains future.
