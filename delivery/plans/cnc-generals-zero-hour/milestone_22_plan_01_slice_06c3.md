# M22 plan 01 slice 06C3: original WW3D camera and static-sort frame scheduler

Requires accepted 06C2. This aggregate is delivered in separately committed A→B1→B2→B3A→B3B→C. `ww3d.cpp` currently excludes its original `Init`/`Shutdown`/`Begin_Render`/`End_Render`/scene `Render` source bodies entirely in the CPU target, while original object `Render` returns early with `IsInitted=false`. Original full `Init(false)` reaches `AnimatedSoundMgrClass::Initialize`, and the existing public pass unconditionally clears fixed color/depth; neither omitted source-owned initialization nor fixed pass defaults can count as the authored frame. C3A translates camera and public viewport; C3B1 retains original WW3D/static-list/audio-source ownership and isolates the physical window/device/audio edges; C3B2 defines explicit public pass load/clear/depth/presentation without altering legacy defaults; C3B3A restores original texture-loader update, texture expiry and memory statistics; C3B3B restores original Begin/End and source full-target clear/alpha sequencing; C3C proves scene/object/static-sort/Flush order and camera-viewport clear. Do not use lite init as a production full-init substitute, duplicate the sound manager, synthesize a frame scheduler, directly mutate `IsInitted` in tests, silently expand partial clear to full target, or fabricate an early pass graph. C4 owns WWShade branch/family audit and aggregate 06C acceptance; 07 must connect the full production `W3DDisplay::init` device/window path before scene integration.
