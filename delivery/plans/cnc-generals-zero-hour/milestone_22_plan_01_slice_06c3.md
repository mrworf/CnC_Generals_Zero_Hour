# M22 plan 01 slice 06C3: original WW3D camera and static-sort frame scheduler

Requires accepted 06C2. This aggregate is delivered in separately committed A→B→C. `ww3d.cpp` currently excludes its original `Init`/`Shutdown`/`Begin_Render`/`End_Render`/scene `Render` source bodies entirely in the CPU target, while original object `Render` returns early with `IsInitted=false`; original `CameraClass::Apply` is a typed physical edge and public `GpuDevice` currently has only full-pass viewport/clear defaults. Thus C3A translates camera and public viewport; C3B restores WW3D lifecycle/clear/presentation semantics; C3C proves scene/object/static-sort/Flush source order. No custom frame scheduler, direct test-only `IsInitted` mutation, silent full-viewport fallback, or fabricated early pass graph. C4 still owns WWShade branch/family audit and aggregate 06C acceptance; 07 production display/scene integration remains future.
