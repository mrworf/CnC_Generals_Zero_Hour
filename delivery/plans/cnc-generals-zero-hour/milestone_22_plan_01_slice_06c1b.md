# M22 plan 01 slice 06C1B: original sorting CPU queue/pool

Requires accepted 06C1A. Restore original `SortingRendererClass::Insert_Triangles`/`Flush` decision body in the canonical `sortingrenderer.cpp`, using the shared source state snapshot/refcount and original sorting vertex/index memory. Preserve source transformed sphere depth, sorted-list order, overlapping sorting pool classification, per-triangle z/indices and source state/material/texture/light refs. Negatives cover disabled-sort direct draw, invalid/out-of-range indices, nonfinite/overflow depth, unsupported buffer type and bounded abort/teardown/requeue. Device-only physical allocation/upload/draw may reject typed after the source-owned CPU decisions; 06C2 must close that edge before any positive sorted pixel/frame claim. GCC/Clang full/LAN, focused sanitizers, original identity/ABI/provider and checked ledger gate a separate B commit; no 06C aggregate acceptance.
