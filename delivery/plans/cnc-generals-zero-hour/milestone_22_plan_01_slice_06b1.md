# M22 plan 01 slice 06B1: canonical original dynamic buffer ownership

Requires accepted 06A3C. Restore original `DynamicVBAccessClass` and `DynamicIBAccessClass` constructors/lock/destruct/reset/deinit in the canonical `dx8vertexbuffer.cpp` and `dx8indexbuffer.cpp` CPU configuration; retain one original source and native Windows branch. The original 5,000-element recycled pool, discard at offset zero, no-overwrite later, bounded 16-bit offsets, in-use exclusivity, underlying `DX8*BufferClass` reference/publication and reset semantics must survive, with Linux CPU storage replacing only the D3D allocation/Lock device edge. Original dynamic buffer bytes (including source FVF `0x252`) and indices must be uploaded through the existing bounded public GPU contract at draw; no adapter-generated geometry, rebase or missing source offsets. Sorting dynamic access remains the original owner and has no positive sorting render acceptance until 06C.

Direct original access fixtures prove first/second allocation, subrange data and offsets, wrap, growth, lock rejection, reset, double acquisition, refcount/bounded destruction and physical upload/index/base offsets on Recording and SDL_GPU Vulkan where applicable. Unsupported layout/range, index/vertex overflow and injected upload/bind errors fail without stale alias or leaked device owner and retry. Retain explicit no-skin/no-decal claim. GCC/Clang full and LAN, provider-removal/link/ABI/source identity, focused sanitizers and validation-layer GPU contract pass before a coherent B1 commit.
