# M22 plan 01 slice 06B4: original decal and complete 06B frame

Requires accepted 06B3. Deliver this unchanged aggregate contract through dependency-ordered 06B4A canonical original CPU decal producer closure, 06B4B source `D3DRS_ZBIAS` to public pipeline depth bias, 06B4C original rigid/skin physical dynamic draws, and 06B4D original Flush interleave/aggregate. Source closure includes `projector.cpp`, `decalsys.cpp`, `decalmsh.cpp` and existing original `MeshClass::Create_Decal`/renderer. Preserve original translucency/APT/culling, UV/color/index and live queue decisions. Source-issued `D3DRS_ZBIAS` must translate to an explicitly bounded public SDL_GPU pipeline depth-bias state (or fail typed for a truly non-encountered route); no arbitrary polygon offset or silent drop. Rigid and skinned decal owners must use original dynamic buffers, world transform and source schedule, not test-created geometry.

Owned authentic W3D fixture proves decals on both reachable rigid/skinned owners, selected depth bias begin/reset, original material/texture and indexed draws with Vulkan pixels and Recording source order. Disabled/translucent/no-APT and invalid-decal/resource/index/bias/failure/retry paths produce correct no-draw or explicit failure, with bounded teardown. Aggregate 06B executes skin, additional/delayed/cull-volume, decal and rigid in original interleaving and confirms every ledger-required operation is accounted for before 06C. Retail-family reachability and unsupported branches are explicit; GCC/Clang full/LAN, provider removal, link/ABI, sanitizers, validation-layer zero VUID pass before each separately committed sub-slice and 06B4D aggregate. No partial 06B acceptance.
