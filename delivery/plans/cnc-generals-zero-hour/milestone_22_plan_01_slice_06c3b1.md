# M22 plan 01 slice 06C3B1: canonical WW3D initialization and teardown

Requires accepted 06C3A. Restore original `WW3D::Init`/`Shutdown` publication in canonical `ww3d.cpp` for the CPU target, preserving full versus lite behavior, source order, `DefaultStaticSortLists`/`CurrentStaticSortLists`, optional DAZZLE.INI and animation-sound-manager configuration, original asset/debug/predictive-LOD release and retry after failure. Link the reached canonical `animatedsoundmgr.cpp` CPU state/INI parser and sound-name/trigger behavior, remove conflicting stubs from `ww3d_cpu_state.cpp`, and keep any actual audio playback/library integration at its separate typed physical edge; do not invent animation-sound configuration or ignore the original manager because audio acceptance is M23. The `W3DDisplay::init` full path must not be replaced with lite init. Preserve source's `IsInitted` semantics (lite does not advertise a full render-ready instance); explicitly reject unsupported external window, native device selection and missing physical attachment pending 07. Tests exercise original full/lite branches using owned file-factory data, static-list identity, sound INI found/missing and source sound lookup, failure/unwind/retry/teardown, no duplicate publication or leaks; full init may use a test-attached public device edge but no proxy manager or direct flag mutation. Original Begin/End and pass clearing remain B3. GCC/Clang full/LAN, focused sanitizers, source/ABI/provider-removal and checked ledger gate independent B1 commit.
